# pnpm 配置（monorepo 根）
# 关闭 safe-delete 批量删除确认：nest build 清理 dist 常超 500 文件阈值，
# 会触发 SAFE_DELETE_BULK_CONFIRM_REQUIRED 导致构建失败（非交互环境无法确认）。
safe-delete=false

# 严格但允许工作区软链接（monorepo 共享包 @web-system/shared）
node-linker=hoisted
