---
description: UI 界面实现规范（admin 系）。任务涉及新建/修改 Vue 页面、admin/deploy-console/mcp-admin 界面、antd 组件外观、颜色/Token、hover、布局、交互、CSS 覆盖时使用。细则本体在 docs/ui/，本规则只负责触发与动作顺序。
alwaysApply: false
enabled: true
updatedAt: 2026-09-21T00:00:00.000Z
---

# UI 界面实现规则（导航 + 动作门）

> 单事实源：先读 `docs/ui/README.md`（读取地图）定位最小文档集，细则在 docs/ui/ 各文档；本规则**不复制数值与细则**（防双份漂移），只规定"触发 + 动作顺序 + 最小禁项"。
> 适用：deploy-console / admin / mcp-admin / 内部工具端。portal / kedou-ai-minigram 品牌端不适用（DR-5）。

## 触发范围（本规则的路径判定，与 PreToolUse hook 判定一致）

- `apps/admin/**`、`apps/deploy-console/**`、`apps/mcp-admin/**` 的 `.vue` 界面文件
- `packages/ui/**`
- 真相源：`specs/kit-sop-enforcement/design.md` §3.2（hook 判定若与此不一致，以设计文档为准并回来修本清单）

## 动作门（顺序不可跳）

1. 读 `docs/ui/README.md`（读取地图）→ 按「任务 × 最小文档集」定位；新页面必读 `docs/ui/design.md`（判断层：类型模板/布局铁律/交互矩阵）。
2. 按 `docs/ui/page-spec-template.md` 填**页面规格书**（新页 Full / 小改 Quick）。
3. **D2 设计评审（交人确认前）**：交 `design-reviewer` 盲审规格书/原型 → 报告落盘 `docs/ui/reviews/<topic>-<YYYYMMDD>.md`（头部 `阻塞: N`）→ **阻塞项清零才可交用户确认**（判据源：`docs/ui/design-system.md`；方案：`specs/design-reviewer/design.md`）。
4. **规格书先给用户过目确认，确认后才写码**（禁止跳过直接实现）。
5. 涉及颜色/加色/hover → 读 `docs/ui/color-reference.md`（颜色定义点地图：8 类位置 + hover 阶梯，禁自行发明色）。
6. 覆盖 antd 外观或"改了不生效" → 读 `docs/ui/css-override-rules.md`（覆盖优先级 + 四问 + 排查五步）。
7. 写完后按 `design.md` §5 自检（grep 裸色/!important、双主题过目、截图基线），修正记录追加到 `docs/ui/geist-token-评审记录.md`（只追加不覆盖）。
8. **D3 实现一致性评审（落码后、提交前）**：比对原型锚点 `data-dr` + 可机检条目 + 截图并置（`docs/ui/baselines/`），报告落盘；阻塞项清零才可提交。存量页面尚未回填锚点时标注「跳过比对」，不判为漂移（方案 §3.7.1）。
9. **批量整改先出清单**：若为规则升格后的批量整改（≥2 文件 / 跨页面 / 跨端），先列《整改影响清单》（文件 / 改动 / 风险 / 优先级）给负责人确认，**不得直接开改**（design.md §5-7，R6 教训）。
10. 落码提交时：UI 源码须与已确认的原型/规格配对 —— 原型或规格先**单独 commit**，其后的 UI commit 带 `Proto: <sha>` trailer **并带 `Design: pass`**（`commit-msg` hook 强制 + CI R10 / **R11（error）** 兜底，见 `specs/kit-sop-enforcement/design.md` §3.8 与 `specs/design-reviewer/design.md` §3.7）。

## 核心约束精要（来不及翻 docs 时的最小禁项）

- 色值只引用 `--ws-*` CSS 变量或 `uiTokens`；禁裸 hex/rgba。新色必须先入 `packages/ui/src/tokens.ts` + `tokens.css`（双文件同步）。
- 禁新增 `!important`（全仓库清零目标，存量见 css-override-rules.md §7）。
- antd 组件外观优先 ThemeConfig 组件 token（`antd-theme.ts`）；单页特例才 scoped `:deep`（引 --ws-*），禁页面级裸 `.ant-xxx` 全局覆盖。
- 字重只用 400/500/600；图标用 SVG（tabler/antd icons），禁 emoji。
- 互斥单选 ≤5 且固定 → `a-tabs`（≤4）/ `a-radio-group`（5 或空间紧），不用 `a-select`。
- hover 分层：容器类（行/菜单/下拉项）用 `--ws-bg-hover`；悬浮触发器（header 用户卡等）**不加整块灰底**。
- 主操作 primary ≤1；破坏性操作必二次确认（文案含后果）；空态 = 原因 + 出路；数字列 tabular-nums、代码/端口/ID 用 `.ws-mono`。
- 页面类型先判定（design.md §1），禁止自创布局骨架。
- dark 主题不是"调暗"：新写样式 light/dark 都要过目，禁漏变量回退。
- 正文与强调分离：长文正文用 `--ws-text-body`，`--ws-text-primary` 只用于标题/强调；代码块统一 `--ws-code-*` 深色方案（三端同值）；行高/字距、动效（150–300ms）、focus-visible、控件/图标阶梯一律取 tokens 对应档（design-system E10–E13 / F6 / G10–G12）。

## 生效提示

- 新建/修改本规则后需**新开会话**生效。
- 验证：向 AI 询问"当前应用了哪些规则"。
- 手动引用：对话中 `@ui-interface`。
