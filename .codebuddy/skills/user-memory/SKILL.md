---
name: user-memory
description: 用户记忆档案 — 记录项目上下文、设计偏好、决策习惯，帮助 AI 在后续对话中更快对齐用户意图。每次重大决策后自动更新。
trigger: 自动加载（用户偏好和项目上下文）
version: 1.0.0
---

# 用户记忆档案

> 本文件由 AI 根据对话历史自动维护，记录关键决策和偏好。每次重大变更后更新。
> 本模板为**通用结构**，加载到具体项目时，把下方占位替换为该项目的真实信息。

---

## 项目身份

| 属性 | 值 |
|------|-----|
| 产品名 | `<产品名>` — `<一句话定位>` |
| 领域 | `<如 技术写作 / 市场文案 / 研究综述>` |
| 资料形态 | `<如 文档库 + 素材库 + 发布渠道>` |

---

## 设计偏好

### 色彩
- **主色**：`<色值>`（`<约束，如非渐变>`）
- **背景/文字**：`<色值>`

### 品牌元素
- `<Logo / 图标规范>`

### UI 风格
- `<如现代克制风、圆角范围、明暗主题>`

### 命名
- `<品牌名/术语约定>`

---

## 架构决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| `<yyyy-mm-dd>` | `<决策>` | `<理由>` |

---

## 工作流偏好

- ✅ `<如大方案先出多选项，确认后再落地>`
- ✅ `<如涉及 UI 先确认设计方向>`
- ❌ `<如不做无依据的设计>`
- ⏳ `<如用户活跃时段>`

---

## 沟通习惯

- 语言：`<如中文>`
- 风格：`<如直接、有依据>`
- 对方案要求：`<如先出选项，标注推荐，给理由>`
- 对产物要求：`<如干净、无冗余>`

---

## 当前资料/模块清单

| 资料 / 模块 | 位置 / 路径 | 用途 |
|------------|-------------|------|
| `<模块>` | `<路径 / 链接>` | `<用途>` |

---

## 记忆更新规则

当以下情况发生时，AI 应更新此文件：
1. 用户确认了新的颜色/设计方向
2. 新增/合并/删除模块或服务
3. 重大架构决策落地
4. 用户明确表达的偏好变更

更新方式：直接编辑本文档对应章节。
