# 智能体定义模板（Agent Definition Template）

> 用法：建设新智能体时**复制本文件**为「新智能体名」-definition.md 并逐节填写，删去示例与说明列。
> 方法论依据：`references/agent-definition-methodology.md`（六维框架 + 统一辨证语言）。
> 评测依据：`references/eval-framework.md`。**定义资产与评测用例捆绑交付，缺评测不算完成。**
> 完整实填样例见附录 A（数字人，用于校验模板可用性）。

---

## 0. 命名与一句话定位

- 名称：（目录/SKILL 名，与 `skills/` 下该技能目录的 frontmatter name 字段一致）
- 一句话定位：（给谁、做什么、不做什么）

## 1. 定位与意图

【待填：用辨证语言，禁形容词】

- 做成 = （一句话可验证定义）
- 不算做成 = （边界 / 反例）
- 驱动方 / 不做损失 = （用户需求 / 业务 / 合规？不做或只做一半的具体代价）
- 能力圈声明 = （核心领域掌握程度；不懂点显式标出）

自检：这节能否被端到端任务卡直接引用为"做成"判据？

## 2. 输入空间与边界（分派决策树）

【待填：先画树，再填其余各维】

```
用户请求
  │
  ├─ 类别 A ──────→ 子技能 A
  ├─ 类别 B ──────→ 子技能 B
  └─ 明确不接的类别 → 出口（显式报错/提示，不自行发明行为）
```

- 边界内请求类别清单（穷举，禁"其他"黑洞）：
- 明确不接什么（给出已定义的出口行为）：

自检：每个分支都有对应产物/出口？边界外有定义好的行为而不是静默？

## 3. 工作流与过程约束

【待填】

- 阶段划分与产物链：（阶段 → 产物 → 下一阶段输入）
- 固定人审节点：（哪几处、审什么、顺序）
- 关键状态与转换：（如 完成声明必须附验证证据）

自检：每阶段产物是否可机器核对存在性？顺序是否 逻辑 → 合规 → 对照 spec？

## 4. 能力集

【待填：每个子能力一张表】

| 子能力 | 触发条件 | 工作流（输入→输出） | 不做什么 |
|--------|---------|--------------------|---------|
| 技能 1 | | | |

- 必备 vs 可后补：（MVP 只做必备）
- 能力间接管关系：（上一个完成后交给谁）

## 5. 红线与机器化

【待填：每条红线 = 约束 + 判定方式；靠自觉的不算红线】

| # | 红线（绝对不做） | 违反判定（机器/清单/陷阱用例） |
|---|----------------|------------------------------|
| 1 | | |
| 2 | | |

自检：反复错误类红线是否已（或计划）机器化？每条红线是否有 L3 陷阱用例？

## 6. 上下文与知识策略

【待填】

- 常驻加载：（固定进上下文的最小集）
- 按需加载：（参考文档表：文档 → 何时加载）
- 项目上下文承载方式：（如 `references/project-context.md`，加载到具体项目时替换）
- 防膨胀策略：（子任务拆分、主线程只留摘要、即时加载）

## 7. 评测交付物（捆绑，缺一不可）

【待填：按 eval-framework 实例化】

- [ ] L2 路由用例 ___ 条（= 决策树分支数，含拒答分支）
- [ ] L3 陷阱用例 ___ 条（每条红线 ≥ 1 个静默违反场景）
- [ ] L4 端到端任务卡 ___ 张（固定输入 + 期望产物清单 + 质量评分点）
- [ ] 首份基线报告（干净上下文，judge 隔离）

## 8. 定稿前辨证（简化档 3 问）

1. 做成 / 不算做成的定义，一句话？（答不出 → 回到 §1）
2. 我预设了什么约束？哪条最可能错？
3. 这个智能体在什么情况下会失败 / 不成立？失败时可观测的现象是什么？

---

## 附录 A：数字人实填样例（第 1 号实例，用于校验模板）

> 校验记录（2026-09-02）：按本模板实填数字人，六维均能对应到 kit 实际组件，无空洞维度；发现唯一返工点是 §2 分派决策树——需先穷举请求类别再填其它维，否则 §4 能力集会漏分支。

### 0. 名称与定位

- 名称：`rd-digital-agent`（Hub）
- 一句话定位：通用数字人 Hub——按任务类型与复杂度自动分派到子技能（brainstorm → plan → execute → review 流水线，外加调试/重构/探索/验证门/审查），让 AI 作为数字同事完成方案探索、内容创作、问题修复等场景。

### 1. 定位与意图

- 做成 = 任意请求被分派到正确的子技能，且该技能按自身工作流产出可追溯的版本化产物
- 不算做成 = 模糊需求未分派直接开工 / 报错未经根因分析直接改代码
- 驱动方 = 团队把 AI 当数字同事，人只在关键节点审核；不做损失 = 每次任务靠对话临场指挥，质量与可追溯性不可控
- 能力圈 = 通用工作流方法论（不绑定技术栈）；项目专属信息由 `project-context.md` 占位承载

### 2. 输入空间与边界（分派决策树）

- 边界内 9 类分支：怎么做/设计方案（brainstorm）· 拆任务/细化（plan）· 报错/测试失败（debug）· 重构（refactor）· 查实现位置（explore）· 小改动/修 bug（execute）· 架构/选型审查（tech-review）· 任何交付前收尾（verification）· 写作/产出（项目自有纪律）
- 明确不接：代码库外业务规则的发明（由人定义）、领域专属术语（由项目上下文提供）

### 3. 工作流与过程约束

- 产物链：intent（需求辨证）→ spec（requirements/design/tasks）→ execute（TDD 迭代-校验）→ review（带审查记录）→ 复盘回灌下一轮
- 人审节点三处：意图确认、设计确认、交付前审查；顺序固定 逻辑 → 合规/红线 → 对照 spec
- 完成声明 = 验证证据（verification-before-completion 门）

### 4. 能力集

| 子能力 | 触发 | 工作流 | 不做什么 |
|--------|------|--------|---------|
| rd-brainstorm | 模糊需求/要方案 | 复述 → 辨证 → 2-4 方案对比 → 推荐 | 不写代码、不定 API |
| rd-plan | 明确方案要拆解 | thinking-checklist → requirements/design/tasks | 不越过确认直接执行 |
| rd-execute | 已确认任务 | TDD 迭代-校验 | 不静默偏离 spec |
| rd-review | 实现完成 | 对照 spec 自检 | 不放过无验证证据的完成声明 |
| systematic-debugging | 报错/意外行为 | 四阶段根因分析 | 禁止报错即改 |
| incremental-refactoring | 重构/清理 | 测试保护下小步重构 | 禁止顺手改行为 |
| code-explore | 查实现/理解结构 | 索引优先 + 影响面分析 | 只读，不改码 |
| tech-review | 架构/选型/安全 | 多维审查清单 | 不评审材料自足度不足的方案 |
| verification-before-completion | 一切交付收尾 | 完成前强制验证 | 不默认"应该没问题" |

### 5. 红线与机器化

| # | 红线 | 违反判定 |
|---|------|---------|
| 01 | 工作以循环组织，阶段产物版本化落盘 | 产物存在性机器核对（check-artifacts.sh）|
| 02 | 三处人审节点固定把关 | L3 陷阱用例 + 任务卡 D3 |
| 03 | 版本化产物必须落盘，不只对话 | 产物清单脚本判定 |
| 04 | 复杂任务拆独立上下文，主线程只留摘要 | L4 任务卡 D5 |
| 05 | 反复错误以机器检查保障；无业务定义不发明兜底 | eval-gate CI + 陷阱用例 |

### 6. 上下文与知识策略

- 常驻加载：AGENT.md（总则 + 定位 + 人审节点 + 红线）
- 按需加载：各 skill 的 references/ 表（spec-workflow 需 spec 时加载、writing-standards 需确认格式时加载、thinking-checklist 辨证时加载）
- 项目上下文：加载到具体团队时替换 `rd-digital-agent/references/project-context.md`
- 防膨胀：多 Agent 协作（brainstorm/plan/execute/review 各独立上下文，返回摘要后释放）

### 7. 评测交付物

- L2 路由用例 20 条（覆盖分派决策树 9 分支）✅ `evals/cases/routing.md`
- L3 陷阱用例 6 条（红线静默违反场景）✅ `evals/cases/behavior.md`
- L4 端到端任务卡 6 张（T1~T6 + 五维 RUBRIC）✅ `evals/golden-tasks/`
- 首份基线报告：✅ 已落盘 `evals/reports/`（2026-09-02）
