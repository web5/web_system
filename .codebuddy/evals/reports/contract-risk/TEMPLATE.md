# 合同翻译官 · E2E 评测报告

复制本文件为 `.codebuddy/evals/reports/contract-risk/<YYYY-MM-DD>/<roundDir>.md` 后填写。
报告是 Agent 行为演进的回归证据，**落盘后不改写历史报告**（防漂移，对齐 eval-framework §4）。
每份样本正文请保留在样本库 fixtures/（本文只给结论与要点）。

---

## 头部信息

| 项 | 值 |
|----|----|
| 评测轮次 roundDir | `<001 等>` |
| 被测模型 | `<模型名 + 版本>` |
| 每样本运行次数 | `<1 或 2，正式建议 2 取均值>` |
| 样本数 | `<n>` |
| 样本集版本 | `<v1 等；首次为 new-baseline>` |
| Rubric 版本 | `<v1；改三层判定 = 重置>` |
| judge 参与 | `<无 / 模型名（须与被测隔离）>` |
| 日期 | `<YYYY-MM-DD>` |
| 评测人 | `<姓名>` |

## 通过率总表

| 指标 | 本版 | 上一版(<hash>) | Δ | 判定 |
|------|------|---------------|----|----|
| 样本通过率 | | | | |
| L1 字段全通过率 | | | | |
| L2 数值准通过率 | | | | |
| L3 红线净通过率 | | | | |
| 单样本均耗 token | | | | |
| 单样本均耗时(ms) | | | | |

> 回归判定：通过率或任一层通过率下降，或 token 均摊上涨 ≥50%（无合理说明）→ 判退化，本次 agent 变更不合并。

## 逐样本明细

| 样本 | scene | L1 | L2 | L3 | 通过 | 备注（亮点/退化/抽检差异） |
|------|-------|----|----|----|------|------|
| <id> | | PASS/FAIL | PASS/FAIL | PASS/FAIL | 通过/不通过 | |

## 逐样本备注

<!-- 每样本一两行：哪项 FAIL、为什么、是 agent 问题还是样本标注问题 -->

- <sample-id>：

## 本轮发现的 agent 问题 → 下一轮 intent

<!-- 可复现的产出缺陷，回灌为 systemPrompt / 工具 / 标准库的下一轮变更 -->

- 合并判定：☐ 可合并 ☐ 修正后重评
- 问题清单：
- 下一轮 intent：
