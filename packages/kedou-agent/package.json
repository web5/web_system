{
  "name": "kedou-agent",
  "version": "0.2.8",
  "description": "科豆 AI Agent CLI —— 基于 @kedouai/agent-core 的交互式 AI Agent（自带大模型 API key，不消耗作者 token）",
  "main": "dist/cli/index.js",
  "types": "dist/cli/index.d.ts",
  "bin": {
    "kedou-agent": "./bin/kedou-agent.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "prepublishOnly": "node scripts/check-secrets.js"
  },
  "keywords": [
    "agent",
    "ai",
    "cli",
    "llm",
    "react",
    "tool-use"
  ],
  "private": false,
  "license": "MIT",
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@kedouai/agent-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
