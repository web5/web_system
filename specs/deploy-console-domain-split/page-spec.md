# 页面规格书 · deploy-console 双域重构（微前端 / API 网关）

> 版本：v1.0（待用户过目）｜ 2026-09-18
> 输入：`./requirements.md`（FR-1~FR-10）+ `./design.md`（数据模型 + 接口契约）
> 规范依据：`docs/ui/design.md`（判断层）、`docs/ui/page-spec-template.md`（Full 模板）
> 原型稿：`docs/ui/prototypes/deploy-console-domain-split.html`
> 适用端：deploy-console（admin 系，主橙 `--ws-brand-500` / DR-3）

---

## 0. 信息架构总览（先定 IA，再谈页面）

### 0.1 导航树（改造后）—— 顶部一级 + 左侧二级（Q13 已确认）

```
┌ 顶部一级菜单（横向，MainLayout topnav）──────────────────────────────┐
│ 仪表盘 │ 微前端 │ API 网关 │ 流水线 │ 基础设施 │
└──────────────────────────────────────────────────────────────────────┘
        │ 点击一级 → 左侧二级菜单（sidebar 随一级切换）
        ▼
  微前端      → 应用管理 /apps · 环境管理 /environments · 版本部署 /deploys/micro
  API 网关    → 服务管理 /services（**无「版本部署」** —— 2026-09-21 对齐落地：选版本部署只挂微前端域）
  流水线      → 流水线列表 /pipelines · 灰度 /canary · 自助诊断 /diagnose · 工具目录 /tools
  基础设施    → 主机管理 /hosts · 服务监控 /monitor
              · 审计 /audit · 通知 /notifications · 配置中心 /config · 系统设置 /settings
  仪表盘      → 无二级，点击直接进入 /dashboard
```

**下线**：`/modules`、`/modules/new`、`/modules/:key`、`/modules/:key/edit`（Q6 已确认直接下线，不做重定向）。

> 导航形态（Q13 已确认 2026-09-18）：**顶部一级菜单 + 点击一级后左侧展示二级菜单**，保持 `MainLayout.vue` 现有顶部菜单形态；「微前端」「API 网关」为两个一级项，其下子项进左侧二级栏。

### 0.2 路由与组件清单

| 路由 | 页面 | 组件 | 状态 |
|---|---|---|---|
| `/apps` | 应用列表 | `AppManager.vue` | 新增 |
| `/apps/:key` | 应用详情 | `AppDetail.vue` | 新增 |
| `/services` | 服务列表 | `ServiceManager.vue` | 新增（原组件名退役，语义归位） |
| `/services/:key` | 服务详情 | `ServiceDetail.vue` | 新增 |
| `/environments` | 环境管理（**微前端域**） | `EnvironmentManager.vue` | 新增（微前端二级菜单；管理站点 + 环境 + 切换列表） |
| `/deploys/micro` | 版本部署（**微前端域**） | `VersionDeploy.vue` | 新增（选已有版本直接部署，与发布流水线解耦；API 网关域不挂该入口） |
| `/hosts` | 主机管理 | `HostManager.vue` | 新增 |
| `/modules*` | — | `ServiceManager.vue` / `ModuleDetail.vue` | **删除** |

---

## 1. 页面规格 · 应用列表 `/apps`

- 页面类型：**列表/管理页**
- 参照页：`ServiceManager.vue`（视觉/Token canonical）+ `UserList.vue`（页头形态）
- 需求一句话：一眼看清有哪些前端产品、它们是什么类型、现在各环境跑着哪个版本。

### 页头

- 标题：**应用管理**
- 副标题：微前端应用与子模块，按产品特性管理；发布与版本在详情页按环境操作。
- 主操作：**新建应用**（primary，右上，≤1）

### 模块清单

| 序 | 模块 | 数据来源 | 承载组件 | 空态策略 |
|---|---|---|---|---|
| 1 | 筛选条（类型互斥切换 + 关键词） | — | `a-tabs`（全部/基座/微前端/独立应用/小程序，≤4 走 tabs）+ 搜索框 | — |
| 2 | 应用表格 | `GET /api/dc/apps?kind=&q=` | `a-table` | "还没有应用，点右上「新建应用」注册第一个（如 portal）" |

### 列清单

| 列 | 字段 | 展示规则 | 操作 |
|---|---|---|---|
| 应用 | `key` + `name` | key 用 `.ws-mono`；name 为说明文字；`parentKey` 有值则在 name 前显示缩进 + 连线标记"子模块" | 点 key 进详情 |
| 类型 | `kind` | 文字徽标（`a-tag` 中性色，**不配色** — R1-1：类型/枚举不配色） | — |
| 仓库目录 | `repoDir` | `.ws-mono` | — |
| 环境版本 | `deployments[]` | 每个环境一个版本胶囊，`local/dev/prod` 并列展示当前版本短 sha（`.ws-mono` + `tabular-nums`）；无版本显示 `--` | — |
| 状态 | `enabled` | 启用/停用（`a-switch`，停用时 tooltip "停用后 shell 不再加载该应用"） | — |
| 操作 | — | 行内 ≤3，超出进 dropdown：详情 / 部署 / 更多（路由、环境、停用、删除） | 见交互清单 |

### 交互清单

| 操作 | 触发 | 反馈 | 破坏性？ | 完成后 |
|---|---|---|---|---|
| 新建应用 | 页头 primary | success "应用已创建" | 否 | 刷新列表并高亮新行 |
| key 重复 | 提交时 | `field` 级错误 "key 已存在" | 否 | 停留表单 |
| 停用 | 行内 switch | success + tooltip | 否（可恢复），不做 Modal | 就地更新 |
| 删除 | dropdown | **Modal.confirm** "删除后该应用的版本记录仅保留历史，不可再发布" | 是 | 刷新列表 |
| 加载失败 | — | `message.error` + 表格内「重试」按钮 | — | — |

### 状态覆盖自查

- [x] 加载中（table loading）
- [x] 空态（原因 + 出路）
- [x] 失败可重试
- [x] 破坏性二次确认（删除）
- [x] 禁用有 tooltip（停用）
- [x] 保存中防重复提交
- [x] 无弹窗套弹窗（新建走 drawer，不套 modal）

---

## 2. 页面规格 · 应用详情 `/apps/:key`

- 页面类型：**详情页**
- 参照页：`ModuleDetail.vue`（保留其 tab 组织方式）+ `PipelineDetail.vue`
- 需求一句话：一个应用的三个面 —— 是什么（概览）、挂在哪（路由）、发到哪（部署）。
- **明确不含**（2026-09-18 用户确认）：「服务环境」（属服务域）、**「环境」Tab**（环境由「环境管理」统一维护，部署 Tab 已按环境展示版本）、前后端混用的「前端 / 后端」切换 Tab

### 页头

- 面包屑：应用管理 / `key`
- 标题：`key`（`.ws-mono`），右侧 `kind` 徽标 + 启用开关
- 副标题：`name` · `repoDir`（`.ws-mono`）
- 主操作：**部署**（primary，触发 `POST /api/dc/apps/:key/deploy`）

### Tab 结构（3 个：概览 / 路由 / 部署）

#### Tab 1 · 概览

| 序 | 模块 | 数据来源 |
|---|---|---|
| 1 | 基本信息卡（key/名称/类型/父应用/仓库目录/入口/publicPath/描述） | `GET /api/dc/apps/:key` |
| 2 | 各环境版本卡（env × 当前版本 × 发布时间 × 操作人） | `GET /api/dc/apps/:key/deployments` |

#### Tab 2 · 路由（shell 挂载）

| 序 | 模块 | 承载组件 | 空态 |
|---|---|---|---|
| 1 | 路由表格（挂载路径 / 激活规则 / 需鉴权 / 排序 / 启用） | `a-table` 可编辑行 | "该应用尚未挂载到 shell，点「新增挂载」配置入口（如 /admin）" |
| 2 | 批量保存条 | 底部 sticky primary 按钮「保存路由变更」 | — |

- 列：挂载路径（`.ws-mono`）/ 激活规则（`.ws-mono`）/ 需鉴权（switch）/ 排序（数字 `tabular-nums`）/ 启用（switch）/ 操作（删除）
- **冲突检测**：`mountPath` 与同 shell 已有路由重复 → 行内红色提示 + 保存按钮禁用 + tooltip 说明冲突方（FR-2.2）
- **FR-2.3**：保存只写配置，不触发发布；按钮文案明确为「保存路由变更」，成功后提示 "已保存，需到「部署」页签发布后生效"

#### Tab 3 · 部署（按环境展示版本）

| 序 | 模块 | 数据来源 |
|---|---|---|
| 1 | 环境版本表：行 = 环境（`dev`/`1`/`2`/`prod`），列 = 当前版本 / 发布时间 / 发布人 / 状态 / 操作 | `GET /api/dc/apps/:key/deployments` |
| 2 | 行内操作（**每行均提供**）：**部署** / 切换版本 / 回滚 | 见下 |

| 操作 | 触发 | 反馈 | 破坏性 | 完成后 |
|---|---|---|---|---|
| 部署 | 行内「部署」 | 带该环境发起发布（写 `taskId`） | 否 | 刷新 |
| 切换版本 | 行内「切换版本」 | **弹窗选目标版本**（下拉：`版本 · 时间 · 发布人`，排除当前版本）→ 确认 | 是（影响该环境加载版本） | 刷新 |
| 回滚 | 行内「回滚」 | **弹窗选回滚到的版本**（下拉，**默认预选上一版本**）→ 确认 | 是（覆盖该环境目录产物） | 刷新 |
| 部署抽屉（选版本部署） | 行内「部署」/ 页头「部署」 | 版本列表**含当前版本且均可选中**（当前版本带「当前」徽标）；选中当前版本部署 → 后端幂等，toast「已是该版本，无需切换」| 否（幂等） | 刷新。变更：2026-09-21——原「当前版本禁选」在唯一版本即当前版本时导致抽屉无可点项（用户反馈）|

- 空态："尚无部署记录，点「发布」发起首次部署"

---

## 3. 页面规格 · 服务列表 `/services`

- 页面类型：**列表/管理页**
- 参照页：`ServiceManager.vue`（同族视觉）
- 需求一句话：一眼看清网关背后有哪些后端服务、它们在各环境是否健康。

### 页头

- 标题：**服务管理**
- 副标题：API 网关背后的后端服务；网关路由与接口清单在各服务详情内管理。
- 主操作：**新建服务**

### 模块清单

| 序 | 模块 | 数据来源 | 空态 |
|---|---|---|---|
| 1 | 筛选条（全部/网关服务/业务服务/MCP 互斥 tabs + 搜索） | — | — |
| 2 | 服务表格 | `GET /api/dc/services` | "还没有服务，点「新建服务」注册第一个（如 auth-service）" |

### 列清单

| 列 | 字段 | 展示规则 | 操作 |
|---|---|---|---|
| 服务 | `key` + `name` | key `.ws-mono` | 进详情 |
| 类型 | `kind` | 中性 `a-tag` | — |
| 仓库目录 | `repoDir` | `.ws-mono` | — |
| 默认端口 | `defaultPort` | 数字 `tabular-nums` `.ws-mono` | — |
| 探活路径 | `healthPath` | `.ws-mono` | — |
| 环境健康 | `deployments[]` | 每环境一个状态点（成功=success 语义色 / 失败=error / 未知=中性 `--`），**只编码状态不编码类型**（R1-1） | — |
| 接口数 | `endpointCount` | `tabular-nums`，点击进详情「接口」tab | — |
| 操作 | — | 详情 / 部署 / 更多（探活、停用、删除） | — |

### 交互清单

| 操作 | 触发 | 反馈 | 破坏性 | 完成后 |
|---|---|---|---|---|
| 新建服务 | 页头 | drawer 表单；`repoDir` 存在性校验失败 → 字段级错误 "发布目录不存在" | 否 | 刷新 |
| 手动探活 | dropdown | 行内 loading → success "healthy" / error + 详情展开 | 否 | 就地更新 |
| 删除 | dropdown | **Modal.confirm** "该服务仍有 N 条启用中的网关路由/接口，无法删除"（列出引用者）→ 阻断 | 是 | — |

---

## 4. 页面规格 · 服务详情 `/services/:key`

- 页面类型：**详情页**
- 参照页：`ServiceDetail` 无先例 → 结构沿用 `AppDetail.vue`（本设计统一两个域的详情页骨架）
- 需求一句话：一个服务的五个面 —— 是什么、怎么被转发、有哪些接口、跑在哪、发到哪。

### 页头

- 面包屑：服务管理 / `key`
- 标题：`key`（`.ws-mono`）+ `kind` 徽标
- 副标题：`name` · `repoDir` · `healthPath`
- 主操作：**构建发布**（primary；点击**原地**打开「发起发布」抽屉，不跳转 —— 2026-09-21 用户提出，见 `docs/ui/page-specs/pipeline-product-logic-v1.md` §12）

### Tab 结构（4 个，用 `a-tabs`；**默认激活「接口」页签** —— Q 已确认「点服务进来就是接口列表」）

#### Tab 1 · 接口（★ 本次新增核心 · **默认激活页签**）

- 顶部工具条：搜索（路径/摘要）、方法筛选（`a-radio-group`：全部/GET/POST/PUT/DELETE/ALL）、**导入**（primary 次位 → drawer 支持粘贴 OpenAPI/清单 + 预览差异）
- 表格列：方法（中性徽标，**不配色**）/ 路径模式（`.ws-mono`，`:id` 占位符高亮）/ 摘要 / 鉴权模式 / 权限码（`.ws-mono`）/ 限流（次/分，`tabular-nums`）/ 来源（manual/openapi/scan）/ 启用
- 交互：新增（drawer）、行内编辑、批量导入（UPSERT，**只补全空字段**，导入结果弹层显示"新增 N / 跳过 M / 补全 K"）、删除
- 空态："尚未登记接口，网关当前按「未登记接口 = 放行」处理；点「导入」批量登记，或手动新增"（呼应 FR-6.4 / Q3）
- **FR-6.6**：表头下方一行说明"此页只登记治理元数据，修改不会改变转发行为"

#### Tab 2 · 概览

- 基本信息卡（key/名称/类型/仓库目录/pm2 进程名/默认端口/探活路径/未登记接口策略 `unknownPolicy`/描述）
- 各环境状态卡（env × 运行时 × 主机 × 端口 × 最近部署版本 × 健康状态）

#### Tab 3 · 网关路由（转发规则）

- 表格列：环境（全环境默认 / 指定 env）/ 路径前缀（`.ws-mono`）/ 剥离前缀 / 重写到 / 上游覆盖 / 超时 ms（`tabular-nums`）/ 鉴权模式 / 优先级（`tabular-nums`）/ 启用
- 交互：新增规则（drawer）、行内编辑、删除（**Modal.confirm**：禁用后最长 60s 内生效，FR-7.4）
- **FR-7.2**：存在前缀包含关系时，表格上方出现 `a-alert` 提示"存在包含关系的前缀（/api 与 /api/auth），按优先级数值小者优先匹配"
- **FR-7.3**：环境列为「全环境默认」时置灰标记；指定环境的行显示环境徽标

#### Tab 4 · 环境与发布（只读指向 + 三个行内动作）

> **2026-09-21 修订（用户反馈「原型稿没更新」）**：原「Tab 4 环境（只读）」与「Tab 5 部署」**合并为本 Tab**。
> 依据：控制台已**取消独立「部署」动作**（部署语义并入流水线 —— restart / verify 两个 action 在发布流水线里，
> 流水线跑完即上线），故「部署记录 + 发布/回滚/查看日志」一屏不再存在。原型与实现同步为 4 个 Tab。

- 表格（**只读**）：环境（+ 环境名 + `prod` 打「生产」标记）/ 运行时（空=继承）/ 目标主机（主机名 + 地址:端口 两行）/ 端口（`tabular-nums` `.ws-mono`，空显示「必填」）/ 副本数 / 上游覆盖（空=「按主机+端口拼装」）/ 状态（`未配置` / `启用`）/ **操作**
- 行内操作（三个，横排）：
  1. **构建发布** —— 原地打开「发起发布」抽屉，环境**锁定为该行 `envId` 并禁用**（只读展示，2026-09-21
     用户要求；页头「构建发布」进入时环境可选、默认第一项）；
  2. **配置指向** —— 直达「环境管理 → 该环境 → 后端服务指向」；
  3. **探活** —— 调用 `POST /api/services/:key/probe`；该行**未配置主机/端口时禁用**（tooltip："未配置主机组或端口，探活会 fail-fast（不回落本机）"）
- 底部说明两段（实现原文）：
  - 各环境「指向」（主机组 / 端口 / 上游 / 运行时）为**只读**，统一在 `环境管理 → 环境详情 → 后端服务指向` 内维护；主机组名与地址在「基础设施 → 主机管理」登记。
  - **构建发布走流水线**：拉码 → 构建 → 投递产物 → 后端由 restart（落地 + 干净重启）/ verify（探活通过后切指针）直接生效 —— 流水线跑完即上线，控制台不再有单独的部署动作。
- **编辑入口在「环境管理 → 环境详情 → 后端服务指向」**（2026-09-18 用户确认：以环境为中心配"指向"），本页只展示
- **B4 约束**：主机为空的行标记 warn 徽标 + tooltip "未配置目标主机，部署将 fail-fast"
- **2026-09-20 补充**：本 Tab 含「配置指向」直达操作，见 §9.1（`hostName` 语义二义裁决 Q17，见 §9.4）

---

## 5. 页面规格 · 环境管理 `/environments`（微前端域）

- 页面类型：**列表/管理页**
- 参照页：`ServiceManager.vue`
- 需求一句话：管理微前端的加载环境（站点 + envId 列表 + 切换插件），每个 envId 对应一个产物目录。

### 页头

- 标题：**环境管理**
- 副标题：微前端加载环境；每个环境一个产物目录，用户在页面用切换插件选择加载哪个环境的 js。
- 主操作：**新建环境**

### 模块清单

| 序 | 模块 | 数据来源 | 空态 |
|---|---|---|---|
| 1 | 筛选条（站点 seg + **搜索** + **分页**） | `GET /api/dc/envs?siteKey=&q=&page=` | — |
| 2 | 环境表格 | 同上 | "尚未创建环境，内置 dev/local/prod 已自动生成" |
| 3 | 切换插件说明（挂件 → **大弹窗选择器**：ID 前置 + 搜索 + 常用置顶） | — | — |

### 列清单

| 列 | 字段 | 展示规则 |
|---|---|---|
| 环境 ID（目录名） | `envId` | `.ws-mono`；内置保留字（dev/local/prod）带「内置」标记；用户创建的为自增数字 |
| 名称 | `name` | — |
| 站点 | `siteKey` | 中性 `a-tag`（dev/local/prod） |
| 类型 | `isProd` | 生产用 primary 徽标，其余中性「开发/本地」（**只编码 prod 语义**，R1-1） |
| 排序 | `sort` | `tabular-nums`，决定切换插件里的顺序 |
| 启用 | `enabled` | `a-switch` |
| 操作 | — | 删除（内置禁用 + tooltip） |

### 交互清单

| 操作 | 触发 | 反馈 | 破坏性 | 完成后 |
|---|---|---|---|---|
| 点击环境 ID | 行内链接 | 进入**环境详情**（基本信息 / 后端服务指向） | 否 | — |
| 新建环境 | 页头 | drawer：**envId 由系统自增（只读展示）**，用户只填名称 + 选站点 | 否 | 刷新 |
| 删除环境 | 行内 | **Modal.confirm**，若仍有该 envId 下的部署记录 → 阻断并列出占用者；内置不可删 | 是 | — |
| 调整排序 | 行内数字 | 就地保存 | 否 | 刷新切换顺序 |

### 5.1 环境详情 `/environments/:envId`（2026-09-18 用户确认新增）

- 页面类型：**详情页**
- 参照页：`AppDetail.vue`（同域详情页骨架）
- 需求一句话：环境是"一套完整运行上下文"——前端按 envId 加载产物目录，后端由该环境指向各服务地址。

**Tab 结构（2 个）**

| Tab | 内容 |
|---|---|
| 基本信息 | 环境 ID（不可改）/ 名称 / 归属站点 / 排序 / 前端产物目录 `/static/modules/<appKey>/<envId>/`；页头带启用开关 |
| **后端服务指向** | 表格：服务（`serviceKey` + 名称）/ **上游地址（网关指向）** / 目标主机 / 端口 / 启用 / 操作「修改」 |

**后端服务指向 · 交互**

| 操作 | 触发 | 反馈 | 破坏性 | 完成后 |
|---|---|---|---|---|
| 修改指向 | 行内「修改」 | drawer/modal：目标主机 + 端口 + 上游地址（留空按 主机+端口 自动拼装） | 否（但影响该环境 API 走向） | 刷新表格 |
| 保存 | modal OK | success「已更新 X 在 Y 的指向」；注明网关缓存 ≤60s 生效 | — | — |

- 空态："该环境尚未配置任何服务指向，将使用服务默认地址"
- 边界：主机留空 → 保存前校验必填（防静默指向 localhost，对应 B4）
- 说明文案：「服务本身的注册（仓库目录 / 探活路径 / 接口清单）在『服务管理』内维护；此处只配该环境的指向」

### 状态覆盖自查

- [x] 加载中 / 空态 / 失败可重试 / 破坏性二次确认 / 禁用 tooltip / 防重复提交

### 备注

- 切换插件（`EnvSwitcher`）**不是本页的组件**，它渲染在 shell（产品页面）上；本页只展示预览示意（详见 `environment-design.md` §5）。
- **切换环境 = 整页重载**（`location.reload()`）：环境改变整套产物与接口上下文，局部更新会造成新旧混杂；重载后停留当前路由。
- envId 规则与回退逻辑见 `environment-design.md` §2.2：系统自增数字、内置保留字、找不到回退 dev。

---

## 6. 页面规格 · 主机管理 `/hosts`

- 页面类型：**列表/管理页**
- 参照页：`ServiceManager.vue`
- 需求一句话：登记部署目标机器（含运行时类型），供服务环境引用。
- 页头：标题「主机管理」，副标题「部署目标主机与主机组，供服务环境引用」，主操作「新增主机」
- 列：主机组名（`.ws-mono`）/ 地址（`.ws-mono`）/ SSH 用户 / 部署根目录（`.ws-mono`）/ 运行时（pm2|docker 中性 tag）/ 标签 / 启用 / 操作
- 交互：新增/编辑（drawer）、删除（有服务环境引用时阻断并列出）、连接测试（loading → 结果）
- 状态：空态"尚未登记主机，服务环境将无法配置目标机器"

---

## 7. 跨域一致性约定（两个域必须一致）

1. **详情页骨架一致**：概览 /（路由|网关路由）/（接口）/ 环境 / 部署 —— 两域 Tab 顺序与视觉一致，降低学习成本。
2. **部署矩阵一致**：应用与服务都用「环境 × 实例」矩阵表达当前状态。
3. **主操作唯一**：每页 primary ≤ 1（列表页 = 新建，详情页 = 部署）。
4. **枚举不配色**：`kind`、`runtime`、`method` 全部中性徽标；只有状态（健康/失败/未知）用语义色。
5. **代码类字段统一 `.ws-mono`**：key、路径、端口、sha、主机、目录。
6. **数字统一 `tabular-nums`**：端口、超时、限流、优先级、副本、排序。

---

## 8. 待确认（全部已确认，2026-09-18）

| # | 待确认 | 结论 |
|---|---|---|
| ~~Q13~~ | 导航落地形态 | ✅ **顶部一级菜单 + 点击一级后左侧展示二级菜单**（保持 MainLayout 顶部菜单形态） |
| ~~Q14~~ | 部署矩阵环境多时的呈现 | ✅ 环境 × 实例矩阵；环境 ≥4 时表格横向滚动（本次环境少，按矩阵做） |
| ~~Q15~~ | 接口页方法筛选控件 | ✅ `a-radio-group`（6 项：全部/GET/POST/PUT/DELETE/ALL） |
| ~~Q16~~ | 流水线导航位置 | ✅ 保持原「流水线」组不动，作为独立一级（流水线列表/灰度/自助诊断/工具目录） |

---

## 9. 补充规格 · 服务环境「IP/端口」配置入口（2026-09-20）

> 触发：服务详情「环境」Tab 里 `local`/`prod` 显示「目标主机 — / 未配置」，而配置入口在别的域，用户找不到；且「到底填主机名还是 IP」在数据模型上是**二义**的。

### 9.0 现状问题（三条，先摆事实）

| # | 问题 | 证据 |
|---|---|---|
| P1 | **入口不可达**：服务详情环境 Tab「未配置」行只有「探活」一个操作，配置入口只存在于底部一段提示文案，需自行去「环境管理 → 列表 → 点进环境 → 后端服务指向」四步 | `ServiceDetail.vue` 环境 Tab `action` 列仅「探活」 |
| P2 | **`hostName` 语义二义（关键）**：实体注释写「FK → `deploy_hosts.name`」（主机**组名**，如 `dev-default`），但上游拼装**直接把它当主机名用**：`http://${hostName}:${port}`。填组名会得到 `http://dev-default:6101` —— 该名字不可解析（真实地址 `175.27.189.123`） | `servers/gateway/.../route-match.ts:114-116`、`deploy-console/.../services.service.ts:642-646`、`deploy-console/.../entities/deploy-service-env.entity.ts:25-27` |
| P3 | **主机管理未落地**：`deploy_hosts` 实体已有，但无 CRUD 接口与前端页面（§6 仅为规划）；于是「目标主机」现在是纯手输 `a-input`（placeholder `如 server-dev / 175.27.189.123`），组名与 IP 混填无人拦 | `servers/deploy-console/src/` 无 hosts 模块；`views/` 无 `HostManager.vue` |

### 9.1 迷你规格 · 服务详情「环境」Tab 加「配置指向」

- 页面/组件：`apps/deploy-console/src/views/ServiceDetail.vue`（Tab `envs`）
- 类型与参照：不变（详情页 + 只读表格，原页面）
- 改动内容：
  1. 操作列改为「**配置指向** / 探活」（≤2 个链接，不进 dropdown）：「配置指向」深链到
     `EnvironmentDetail` 且 `query: { tab: 'backend', service: <svcKey> }`
  2. 未配置行（`!record.configured`）：「探活」**禁用 + tooltip**「未配置目标主机，探活会 fail-fast」
  3. 「目标主机」列：已配置时显示 `hostName`，下方小字显示解析出的上游（复用现有 `upstreamUrl ?? host:port`）
- 涉及 Token：状态徽标沿用 `a-badge status=warning`；未配置的「配置指向」文字用 `--ws-color-warning`（**不新增色值**）；代码/端口一律 `.ws-mono` + `--ws-text-secondary`
- 交互影响：无新增状态；补「禁用有 tooltip 原因」一格

### 9.2 迷你规格 · 环境详情「后端服务指向」Tab

- 页面/组件：`apps/deploy-console/src/views/EnvironmentDetail.vue`（Tab `backend`）
- 类型与参照：不变
- 改动内容：
  1. **目标主机列**：主行 `.ws-mono` 显示**主机组名**，副行小字显示该组解析出的地址（IP/域名）—— 让「组名 ≠ 地址」一眼可见
  2. **「修改」弹窗 · 目标主机**：纯 `a-input` → **`a-select` 下拉（选项来自 `GET /api/dc/hosts`）**，选项文案 `<name> · <host>`，**不允许自由输入**（未登记的主机组一律先去主机管理登记，杜绝手输错 IP）
  3. **端口**：`a-input-number` **必填**（1–65535），`placeholder`「按该环境实际启动端口填写」；**不再显示/回退「继承服务默认端口」**（Q19）
  4. **上游地址**：选填，填了即覆盖「主机解析地址 + 端口」；一般留空
  5. **深链接收**：`?service=<svcKey>` → 滚动定位到该行并一次性高亮（行底色 `--ws-brand-100`），**不自动弹窗**（避免弹窗套弹窗）
  6. Tab 顶新增一行 `a-alert` 说明：「主机组名引用『主机管理』，转发地址由主机解析得出；端口按该环境实际启动端口填写，不继承」
- 涉及 Token：`--ws-brand-100`（行高亮）、`--ws-text-tertiary`（副行小字）
- 交互影响：补「保存中防重复提交」「主机未登记 → 400 明确报错」两格

---

## 10. 补充规格 · 时间显示口径统一（2026-09-21）

> 触发：用户在看「应用详情 → 部署」时发现发布时间比系统时间**早 8 小时**。
> 排查结论：**DB 侧无偏差**（本地 MySQL `@@time_zone=SYSTEM`（CST），`deploy_app_env_versions.deployed_at` 与 `NOW()` 同秒；全库 259 个时间列扫描无「未来时间」）；
> 偏差出在**前端渲染** —— 后端 `Date` 经 JSON 序列化为 ISO 字符串（UTC，如 `2026-09-21T10:49:17.000Z`），页面**原样输出**，未做本地化。

**缺陷点（本次修复）**：`apps/deploy-console/src/views/AppDetail.vue` 两处直接渲染 `deployedAt`

1. 概览 Tab · 环境卡片 meta：`{{ e.deployedAt ? e.deployedAt : '尚未发布' }}`
2. 部署 Tab ·「发布时间」列：`{{ record.deployedAt || '—' }}`

**口径（与本页原型部署 Tab 的 `09-18 10:02` 一致）**：所有面向用户的时间一律**本地时区格式化后展示**，禁止直接输出后端原始时间串（ISO/UTC 或裸 datetime）。

- 统一格式：`YYYY-MM-DD HH:mm:ss`（dayjs）；表格单元格沿用 `.ws-tabular`
- 毫秒时间戳字段（`startTime` / `endTime`）沿用既有 `formatTime`（dayjs）口径，不改
- 涉及 Token：无（仅文本内容）；复用既有 `.ws-tabular`
- 影响面复核：仅 `AppDetail.vue` 命中；`AuditLog` / `Dashboard` / `NotificationCenter` / `CanaryCenter` / `VersionDeploy` / `PipelineCenter` / `PipelineDetail` 均为 dayjs 或 `new Date()` 本地化，无同类问题

### 10.1 追加（2026-09-21 同日）：bigint 毫秒时间戳必须先转「数值」再格式化

> 触发：用户看流水线列表「最近执行」，红框时间是 `04-20 10:05:04` / `04-01 14:58:09` / `03-17 15:00:00` —— 而实际发布时间就在今天。

**现象 vs 期望**：页面显示 `04-20 10:05:04`，期望 `2026-09-21 18:49:17`（`MM-DD` 格式把错误的年份 **1797** 藏住了，肉眼只像"4 月"）。

**根因（已实测复现）**：`deploy_pipeline_runs.start_time` 是 **bigint** 毫秒时间戳，经 TypeORM/mysql2 → JSON 到前端是**字符串**（`"1789987757654"`）。`dayjs("1789987757654")` 命中 dayjs 的字符串解析分支，按 `YYYYMMDDHHmmss` 误解析：

| 真实值（毫秒） | 传 number（正确） | 传 string（现状，错） |
|---|---|---|
| `1789987757654` | 2026-09-21 18:49:17 | **1797-04-20 10:05:04** |
| `1789978962589` | 2026-09-21 16:22:42 | **1797-04-01 14:58:09** |
| `1789977387000` | 2026-09-21 15:56:27 | **1797-03-17 15:00:00** |

（三个真实值逐一对应截图三行 —— 根因确认。）

**新增判据**：任何 bigint 毫秒时间戳，在**格式化和比较之前**必须先 `Number()` 归一；禁止把可能为字符串的时间戳直接交给 `dayjs()` / `new Date()`。

- 修复点：
  1. `apps/deploy-console/src/components/pipeline/pipeline.stages.ts` —— `formatTime` / `formatTimeShort` / `durationMs`（共用，PipelineDetail、StageCommandDrawer 一并受益）
  2. `apps/deploy-console/src/views/PipelineCenter.vue` —— 本地 `formatTime` + `latest.startTime` 比较（字符串字典序比较虽在同长度下等价，仍显式归一）
  3. `apps/deploy-console/src/views/Dashboard.vue` —— 失败列表时间（该接口后端已 `Number()` 归一，属同口径下游对齐，避免 `new Date(字符串)` 变 Invalid Date）
- 说明：减法/排序因 JS `ToNumber` 隐式转换原本侥幸正确（截图里排序结果是对的），故本次只修**显示**，不动排序语义
- 涉及 Token：无

### 9.3 前置依赖 · 主机管理 `/hosts`（最小版）

- §6 已有完整规格；本期只落地**最小集**，够支撑上面两个页面：
  - 后端：`GET|POST|PUT|DELETE /api/dc/hosts`（`deploy_hosts` 实体已有，缺 module/controller/service）
  - 前端：`HostManager.vue` + 路由 `/hosts`，列：主机组名 / 地址 / SSH 用户 / 部署根目录 / 运行时 / 启用
  - 种子数据（**组名规范 `<env>-default`，地址存 IP**）：`local-default(127.0.0.1)`、`dev-default(175.27.189.123)`、`prod-default(106.52.176.246)`
  - 存量纠正：现 `deploy_hosts` 三行的 `name` 直接用了 IP（`127.0.0.1` / `175.27.189.123` / `106.52.176.246`），需按上表**重命名为组名**（`name` 是引用键，`host` 才是地址）
- 风险：地址属敏感信息，列表页不做脱敏（内部工具端）；编辑需权限码（沿用现有 `@RequirePermission` 惯例，具体码待定）

### 9.4 决策（2026-09-20 用户确认）

| # | 议题 | **结论** |
|---|---|---|
| **Q17** | `host_name` 存什么？ | **方案 D（规范，不做中间方案）**：`host_name` = **主机组名，外键引用 `deploy_hosts.name`**；转发地址**不冗余存储**，由 `deploy_hosts.host` 解析（IP 只在一处维护，换机器只改主机管理）。`upstream_url` 仅作**显式覆盖**通道。gateway 侧新增 `deploy_hosts` 只读实体映射，缓存里建 `name → host` 索引。<br>✗ 不采纳：新增 `host_address` 冗余列（A）、`host_name` 直接存 IP（B）、只填 `upstreamUrl`（C） |
| **Q18** | local 环境的服务跑在哪？ | **全部本机** → 主机组 `local-default`（`127.0.0.1`） |
| **Q19** | 端口怎么定？ | **不继承、不回退**：`deploy_services.default_port` 不作为回退值；`port` **必填**（1–65535），后台服务实际启在哪个端口就填哪个，人工维护。缺端口/端口错 = 配置错误，**探活与转发都报明确错误**（`服务 X 在环境 Y 未配置端口` / 探活失败原文），不静默回落到 80 或默认端口 |
| **Q20** | 填错怎么办？ | **报错，不兜底**：`host_name` 必须已登记且启用（否则 400「主机组 X 未在主机管理中登记」）；探活/部署失败返回目标地址 + 原始错误，便于人对账 |

> 佐证 Q19：实测 `auth-service` 在本机启 **6101**（6001 被占用）、在 dev 机启 **6001**、在 prod 机启 **3001** —— 同一服务三个环境三个端口，继承必然出错，故必须显式填。

### 9.5 交互清单（合并）

| 操作 | 触发 | 反馈 | 破坏性？ | 完成后 |
|---|---|---|---|---|
| 配置指向（服务详情） | 行内链接 | 跳转环境详情并高亮该服务行 | 否 | — |
| 探活（未配置行） | 行内链接 | 禁用 + tooltip 说明 fail-fast 原因 | — | — |
| 修改指向（环境详情） | 行内「修改」 | modal：主机**下拉（只允许选已登记主机组）** / 端口（必填）/ 上游（选填覆盖）/ 运行时 | 否（改的是该环境 API 走向，提示 ≤60s 生效） | 刷新表格 |
| 保存 | modal OK | success「已更新 X 在 Y 的指向」；失败 → 字段级/消息级**明确错误**（主机未登记 / 端口缺失或越界） | 否 | 刷新 + 清高亮 |
| 转发时主机组被删/停用 | 请求命中 | gateway 502 + 明确原因「主机组 X 未登记或已停用」（**不回退本机**） | — | — |

### 9.6 状态覆盖自查

- [x] 加载中 / 空态（"该环境尚未配置任何服务指向"）/ 失败可重试
- [x] 破坏性二次确认（本期无删除动作）
- [x] 禁用有 tooltip 原因（未配置行的探活）
- [x] 保存中防重复提交（`saving` 锁）
- [x] 无弹窗套弹窗（深链只高亮不自动弹窗）

### 9.7 风险

- **Q17 已定（方案 D）**：`host_name` 改引用后，**存量数据必须同步改写**，否则 `http://dev-default:6010` 这类不可解析上游会继续生效；改写清单见 `./service-env-config-inventory.md`。
- **一次性落地**（①②③ 同批）：依赖顺序仍为「主机 CRUD → 环境详情下拉 → 服务详情直达」，但**不拆两批发布**。
- `deploy_hosts`（新）与旧 `deploy_servers`（`server/` 模块，含 `staging-default`）两套主机数据并存：**以 `deploy_hosts` 为准**，`staging-default` 不迁移（`deploy_envs` 无 staging），其残留指向行一并清理。
- DB 路由当前 **默认关闭**（`GATEWAY_DB_ROUTES≠1`），本次入库不会立即改变线上转发；开启前需先跑一遍全环境探活。

### 9.8 配置清单（下一步：梳理 → 确认 → 入库）

见 **`./service-env-config-inventory.md`**（主机组 × 服务 × 环境 × 端口的待确认清单与入库脚本草案）。

---

## 变更日志

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-18 | v1.0 | 初稿。IA 总览 + 6 个页面规格 + 跨域一致性约定，待用户过目 |
| 2026-09-18 | v1.1 | Q13–Q16 确认：顶部一级 + 左侧二级导航、服务详情默认激活「接口」页签、接口方法筛选用 radio-group、流水线保持独立一级 |
| 2026-09-18 | v1.2 | 导航收拢：仪表盘置顶为第一个一级项；审计/通知/配置中心/系统设置并入「基础设施」二级菜单 |
| 2026-09-18 | v1.3 | 环境管理归属纠正：从基础设施移到**微前端二级菜单**（环境=微前端加载维度，见 environment-design.md） |
| 2026-09-20 | v1.4 | 新增 §9「服务环境 IP/端口配置入口」：服务详情环境 Tab 加「配置指向」直达、环境详情目标主机改下拉+展示可解析地址、主机管理最小版前置；暴露 `hostName` 组名/地址二义（Q17）并给出三方案 |
| 2026-09-20 | v1.5 | Q17–Q20 定稿：`host_name` = 主机组名（引用 `deploy_hosts.name`，地址不冗余，由主机解析）；local 全跑本机；**端口必填不继承、填错报错**；主机下拉不允许自由输入；①②③ 一次性落地；配置清单拆到 `service-env-config-inventory.md` |
