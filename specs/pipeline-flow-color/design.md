# 流水线详情页 · 运行态连线着色规则 v2（设计稿 · 待确认）

> 状态：**提案，待用户拍板**。确认前不改实现代码。
> 原型演示：`docs/ui/prototypes/pipeline-env-branch-canvas.html` →「**详情页 · 执行流程** · 连线着色规则」区块
> （四场景可切换；演示的是**详情页只读画布**——步骤卡带「命令」链接、任务卡只有
> 序号竖条/名称/审批·条件小标/状态文字，无删除按钮与动作行；同文件上方画布为编辑页，连线结构同构）。

## 0 背景与目标

详情页「执行流程」连线着色（走过路径绿色高亮）此前没有整体定义过：规则散落在实现里，
2026-09-22 定过一条「分叉竖线：目标分支存在 skipped → 整段灰」，2026-09-23 又修过一次
「支线只排除 skipped → 未执行也提前变绿」。两次都是局部打补丁，缺一张完整的规则表，
导致连续两轮「看着不对」的反馈。本设计把规则整体定义 + 出可切换场景的原型演示，
确认后再一次性落码。

## 1 问题（真实实例）

| 实例 | 状态 | 现象 |
|---|---|---|
| #3373（成功） | 4-1 local 条件未命中 → 跳过；4-2 dev 成功 | v1 竖线规则「存在 skipped 分支 → 整段灰」→ **4-2 的成功路径入口是灰的**，用户反馈「4-2 执行成功了，怎么还是灰色」 |
| （运行中实例） | 停在「发布确认 · 待审批」 | v1 支线判据「非 skipped 即绿」→ 4-1/4-2 未执行却提前变绿（2026-09-23 已修，判据改为按数据状态） |

根因：规则从未被整体确认；「走过路径」在条件分叉场景下语义模糊（一条分支没走 = 整条路没走过？）。

## 2 规则 v2（提案 · 待拍板）

状态集沿用 `specs/pipeline-task-status/design.md` §2（任务级：succeeded / failed / running /
awaiting / skipped / cancelled / 未执行`''`）。步骤聚合规则不变（§4.2）。

| 连线元件 | 着色条件 | 颜色 |
|---|---|---|
| 主线横线（列 i → i+1） | 进入侧（前一列）步骤聚合 = succeeded | 绿 `--ok`，否则灰 |
| 分叉竖线 | 进入侧 succeeded **且分叉已到达（任一目标任务状态非空）** | 绿 `--ok`，否则灰 |
| 支线横线 + 箭头 | 跟**目标任务自身状态**：succeeded | 绿 `--ok` |
| | failed | 红 `--danger`（v1 为灰，提案升级，待拍板） |
| | running | 蓝 `--info`（新增 token，待拍板） |
| | awaiting | 橙 `--primary`（同「待审批」徽标色，待拍板） |
| | skipped / cancelled / 未执行 | 灰 `--border` |

核心变化：

1. **竖线从「无 skipped 分支才绿」改为「分叉已到达即绿」** ——
   一条分支条件未命中（跳过）不再把另一条真实走通的分支拖灰。
2. **支线从「非 skipped 即绿」改为「按目标任务状态着色」** ——
   未执行/待审批不再提前变绿（2026-09-23 已先修），失败标红、执行中标蓝为提案新增。

## 3 场景矩阵（原型演示的四个场景）

| 场景 | 步骤/任务状态 | 期望着色 |
|---|---|---|
| S1 运行中 · 停在发布确认 | git 完成、build 完成、审批门禁 待审批、4-1/4-2 未执行 | 主线 1→2→3 绿；3→4 主线/竖线/支线全灰（未进入不提前绿） |
| S2 成功 · 命中 dev | 前三步完成、4-1 跳过、4-2 完成 | 主线 3→4 绿；**竖线绿**（分叉已到达）；4-1 支线灰；4-2 支线绿 |
| S3 失败 · dev 动作失败 | 前三步完成、4-1 跳过、4-2 失败 | 主线 3→4 绿；竖线绿；4-1 支线灰；**4-2 支线红** |
| S4 执行中 · dev 运行中 | 前三步完成、4-1 跳过、4-2 执行中 | 主线 3→4 绿；竖线绿；4-1 支线灰；**4-2 支线蓝** |

## 4 与 v1 的差异汇总

1. 竖线：`无 skipped 分支才绿` → `存在已执行（succeeded/failed）分支即绿`（修 S2 反馈）。
2. 支线：`非 skipped 即绿` → `按目标任务状态着色`（修运行中提前变绿，2026-09-23 已先修）。
3. 新增可选着色：failed 支线红、running 蓝、awaiting 橙（v1 全灰）。

## 5 待拍板项

1. 竖线规则 v2（跳过分支不再拖灰，分叉已到达即绿）是否认可？
2. failed 支线标红（信息量更高）还是保持灰？
3. running / awaiting 支线着色（S4 中支线蓝）是否认可，还是一律灰（只有走过才上色）？
4. cancelled（取消）按灰处理是否可以？
5. 步骤标题卡跟随聚合状态着色边框（现状已如此，确认保留）？

## 6 落地范围（确认后）

- 仅 `apps/deploy-console/src/components/pipeline/ProgressFlow.vue` 的 `drawWires()` 着色判定
  与 `taskStateOf()` 复用，不动布局/结构；原型演示区块的规则即实现规则。
- 落码后按 §3 场景矩阵做 D3 实现一致性比对。
