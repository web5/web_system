# 部署环境配置模板
# 复制为 .env.dev 或 .env.prod 并填入实际信息

# 目标服务器（支持 user@host 格式）
SERVER="user@host"

# 远程项目目录
REMOTE_DIR="/data/web_system"

# 公网访问地址（用于部署后健康检查）
PUBLIC_URL="https://your-domain.com"

# SSH 跳板机（如需要，密钥登录则留空）
JUMP_HOST=""

# SSH 密码（密钥登录则留空）
SERVER_PASSWORD=""
