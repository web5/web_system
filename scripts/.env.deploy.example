# ===========================================================
# 部署环境配置文件
# 复制此文件为 .env.deploy，填入实际服务器信息
# ===========================================================
# 注意：此文件包含服务器密码，不要提交到 Git！
# 已在 .gitignore 中忽略 .env.deploy
# ===========================================================

# ---- 开发环境 (dev.kedouai.com) ----
DEV_SERVER="ubuntu@175.27.189.123"
DEV_REMOTE_DIR="/data/web_system"
DEV_PUBLIC_URL="https://dev.kedouai.com"

# ---- 生产环境 (portal.kedouai.com) ----
# 请填入你的现网服务器信息
PROD_SERVER="root@106.52.176.246"
PROD_REMOTE_DIR="/data/web_system"
PROD_PUBLIC_URL="https://portal.kedouai.com"

# ---- 通用配置 ----
# 本地项目根目录（自动获取，一般无需修改）
# PROJECT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

# ---- 数据库连接（sync-schema.sh / seed.sh 读取，禁止硬编码到脚本）----
# dev 库：经 kedou-dev 跳板连服务器本地 MySQL
DEV_DB_HOST=127.0.0.1
DEV_DB_PORT=3306
DEV_DB_USER=root
DEV_DB_PASS=web_system_root_2026

# prod 库：经 kedou-prod 跳板连内网 172.16.16.10
PROD_DB_HOST=172.16.16.10
PROD_DB_PORT=3306
PROD_DB_USER=root
PROD_DB_PASS=gn%!CTvZNP0e4%Lc
