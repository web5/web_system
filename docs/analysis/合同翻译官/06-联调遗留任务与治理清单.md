# 合同翻译官 contract-risk 链路：联调遗留任务与治理清单

> 状态日期：2026-09-07
> 范围：小程序合同体检（`contract-risk`）端到端链路联调收尾。
> 结论：**主链路功能已跑通并验证**（DB 定义 v18 平衡版，示例贷款 + 瓜子融资租赁均产出真实报告）。
> 本文档记录「已解决根因」备忘 + 遗留任务（P0 治本 / P1 真机验证 / P2 质量收尾），供后续开工直接照做。

---

## 0. 当前链路最终形态（本次修复后的运行态）

```
小程序 (apps/mini-app/services/contract-api.ts)
  wx.request(enableChunked) → TextDecoder(stream) 跨 chunk 保真 → success 整包兜底(模拟器)
  → parseReport → extractJsonObject(最长候选) → contract_report → result 页
Gateway :8080 /api/ai-agent/agent/run ── 透传 JWT
ai-agent :6010 /agent/run
  └─ AgentRunner 读 DB 定义（ai-service agent_definitions 每 30s sync 覆盖，DB 优先）
     contract-risk  v18 · model=deepseek-v4-flash · 4 tools · maxSteps=20 · 平衡版 prompt(1156字)
     工具序列：contract-rule → contract-irr → contract-benchmark（按需，不得重复调）
4 Tool（注册于 ai-agent agent.module.ts）→ 确定性计算收口 @web-system/shared
final 事件 = 完整 JSON 报告（前置中文思考可容忍，前端提取最外层 JSON）
```

### 本次联调踩坑与修复备忘（防止回归，改代码前必读）

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 跑的一直是旧逻辑（hy3/3tools/精简 prompt） | agent 定义双源：DB `agent_definitions` 每 30s upsert 覆盖代码内置定义 | 把 DB 定义更新为平衡版（脚本 PUT+publish） |
| 2 | 长 prompt 下工具循环失控、终轮空 final | 旧 6 问 prompt(3173字) + 强制 ```json 围栏 + 无工具纪律 | 发布平衡版 prompt(1156字)：明确「不得重复调用 / 直接输出 JSON 不要围栏 / 总量 <2500 token」 |
| 3 | 瓜子合同跑满 12 步没 final | 模型反复调 irr 试不同本金假设 | prompt 加「参数用合同明确数字不要反复试」+ maxSteps 12→20 |
| 4 | run 已完成但小程序一直 loading | (a) TextDecoder 每次 new，跨 chunk 中文切断→整行 parse 失败 final 被丢；(b) 1710 次 onDelta 全量 setData 拖死 UI | (a) `createChunkDecoder()` stream 解码+字节累积兜底；(b) onDelta 160ms 节流+尾部 3000 字符窗口 |
| 5 | 模拟器没任何 chunk（仍 loading） | 开发者工具基础库对 enableChunked 支持不全，onChunkReceived 不触发，整包进 success 但旧代码 success 为空函数 | `success(res)` 加整包按 SSE 协议兜底解析（初次分析+追问两处） |
| 6 | final 有内容但 result 页「0 风险/分析完成」兜底 | 模型在 JSON 前加中文思考；`extractJsonObject` 从右往左扫配对，**先命中内层嵌套子对象**（如 signals[].legalBasis）当顶层返回 | 改为**挑最长合法候选**（最外层 JSON），两处（正常+截断容错）都改 |
| 7 | （发现）DB model 写 `deepseek/deepseek-v4-flash` 带 vendor 前缀 | `agent-def-sync.toAgentDefinition` 不做前缀归一 → `AgentEngine` 用 `getOrFallback('deepseek/deepseek-v4-flash')` miss → **静默回退 hy3**（registry 注册名是无前缀 `deepseek-v4-flash`） | 统一命名：DB/seed 存裸 id `deepseek-v4-flash`，或在 engine/sync 增加 vendor 前缀剥离（二选一，建议存裸 id）；当前实际效果 = contract-risk 一直在 hy3 上跑，平衡版仍稳定，故未暴露 |

---

## 1. P0 — 治本：代码 seed 与 DB 单一事实源对齐

**背景**：DB 是唯一事实源，但代码里还有两份「旧定义」会在环境重建/DB 清空时兜底回归成坏版本，必须同步为 DB 当前权威版。

### 权威快照（DB 现状，同步时的源）

```
model: deepseek/deepseek-v4-flash · tools: [cleaner, rule, irr, benchmark]
maxSteps: 20 · temperature: 0.3 · version: 18 · status: published
systemPrompt: 平衡版 1156 字符（含「不要重复调用」「不要 ```json 围栏」「总长<2500 token」纪律）
```

重新拉取命令：
```bash
curl -s http://127.0.0.1:6003/internal/agent-definitions | python3 -m json.tool
```

### T1. 同步 `ai-service` 的 builtinSeeds 中的 contract-risk（防 DB 空表重建回归）

- 文件：`servers/ai-service/src/agent-def/agent-def.service.ts` → `builtinSeeds()`（约 305-327 行）
- 现状：`hy3` / 3 tools（缺 benchmark）/ 精简旧 prompt / `maxSteps: 12`
- 动作：把该条与 DB v18 对齐 —— `model: 'deepseek/deepseek-v4-flash'`（**照抄 DB 值**，勿改裸名）、tools 补 `contract-benchmark`、`maxSteps: 20`、systemPrompt 换 DB 当前全文
- 注意：`model` 在 DB 中带 vendor 前缀 `deepseek/`，而 agent-core 注册名是 `deepseek-v4-flash`；目前运行正常说明有归一逻辑。**seed 值照 DB 抄最安全**，不要另造格式。
- 验收：清空 `agent_definitions` 表 → 重启 ai-service → seed 出的 contract-risk 与 DB v18 完全一致。

### T2. 治理 `ai-agent` 代码内置定义（deepseek-chat 已下线，剩余：prompt 同步）

> 2026-09-07 已执行：官方直连 `deepseek-chat` 全线下线，DeepSeek 统一走 TokenHub。
> 涉及文件 model 均已改为 `deepseek-v4-flash`：`contract-risk.agent.ts`、`contract-cleaner.tool.ts`、
> `ocr.service.ts`、`ocr.module.ts`（注册改为 `TokenHubClient('deepseek-v4-flash')`）、`deploy.agent.ts`、
> `servers/ai-service/src/agent/agent.module.ts`（同注册替换）；agent-core `DeepseekClient` 打「已弃用」标记
> （暂时保留仅兼容 kedou-agent CLI 独立子项目）。lint 已全部通过。

- **剩余动作**：`contract-risk.agent.ts` 代码内置的 systemPrompt 仍是**旧 6 问长版**（只改了 model）。作为"无 DB 兜底"它仍可能翻车（长 prompt 工具循环失控）。建议把 systemPrompt 同步为 DB v18 平衡版全文（1156 字，见 §1 权威快照）。`maxSteps` 代码侧 15 也应升到 20。
- 决策选项不再涉及 model（已统一），仅剩：
  - **A（推荐）**：代码内置保持 = DB v18 平衡版（同步 prompt + maxSteps 20），双源一致，30s 覆盖无害，无 DB 时兜底不翻车。
  - **B（演进注释方向）**：删代码内置定义，DB 唯一事实源（需另立架构任务评估 ai-agent 无 DB 兜底风险）。

---

## 2. P1 — 真机验证（需微信开发者工具真机预览 + 人工操作）

模拟器 enableChunked 不可靠，已用 success 兜底让模拟器能出结果，但**真机流式体验与相机链路未验**。

| # | 任务 | 操作 | 观察点 |
|---|---|---|---|
| T3 | 真机 SSE 实时流式回归 | 开发者工具「预览」扫码（真机与后端同局域网，确认 apiBase 可达）；跑贷款示例 + 瓜子合同各一次 | analyzing 页应实时滚尾部 JSON（非 40s 后整包跳出）；`[SSE] chunk#` 日志连续；成功跳 result 出报告 |
| T4 | 追问链路 UI 实测 | result 页点「我想追问」并发送 | 请求体带 conversationId；返回上下文相关回答（非重新分析）；流式渲染正常 |
| T5 | OCR 拍照链路实测 | 真机上传页拍照/相册选合同 → 识别 → 分析 | `/api/ai-agent/ocr/recognize` 200 且正确识别；`[SSE]` 链路上出现 contract-cleaner 工具调用；（模拟器此前「令牌无效或已过期」为无相机假象，真机应不复现） |

---

## 3. P2 — 质量收尾（提审/上线前）

| # | 任务 | 位置 | 说明 |
|---|---|---|---|
| T6 | 关闭 SSE 本地调试日志 | `apps/mini-app/services/contract-api.ts:19` `SSE_DEBUG = true → false` | 文件注释已写明；联调完成必须关，防刷 Console/拖慢低端机 |
| T7 | 复核 analyzing 节流策略 | `pages/contract/analyzing/analyzing.ts` onDelta（160ms 节流 + 尾部 3000 字符窗口） | 是为保模拟器 UI 加的；真机验完 T3 后决定是否恢复全量流式展示或保留尾窗 |
| T8 | 联调账号/密钥治理 | auth-service seed 账号 | 本次使用：`mp_contract_0907`（user）、`admin/admin123`（本地 ADMIN_INIT_PASSWORD）。上线/换环境前确认 admin 弱口令已改、测试账号已隔离 |
| T9 | agent 定义后续维护通道规范化 | ai-service / admin Agent 管理 | 本次 v18 是脚本 PUT+publish 直写 DB；后续改 prompt 建议走 admin 页面或固化「改代码 seed → 重启 ai-service → 校验同步」流程，避免改 DB 无代码记录 |

---

## 4. 待拍板/待确认事项汇总

1. **T2 方案 A vs B**（agent 定义是否保留代码内置兜底）
2. T3/T5 是否安排一次真机回归（需局域网环境 + 真机）
3. 平衡版 prompt 是否需进一步产品化打磨（结论段≤25字、signals≤5 条等硬约束是否满足业务 KPI；见 `docs/analysis/合同翻译官/03-系统提示词升级设计.md` 的后续演进方向）
