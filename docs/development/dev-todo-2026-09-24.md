# 待办清单 · dev/prod 基础设施（2026-09-24）

> 开关：CHANGELOG=off · HISTORY_NOTE=off · FAQ_KEEP=off

> 定位：dev/prod 基础设施与发布链路的**跟进清单**（谁该做、卡在哪、做完算什么）。
> 环境事实、踩坑与命令速查不在此处，见 `session-handover-2026-09-23.md`。

---

## A. 阻塞中 —— 需用户决策或提供信息

### A1 · prod 的 upload-service 上线（P1，**卡住 A8 上 prod**）

- 现状：prod 有 dist 但未运行、`.env` 为空、端口未登记。
- 依赖项：`PORT`（建议 3008，dev 侧是 6008）、`INTERNAL_API_KEY`（须与 ai-service 完全相同）、`JWT_SECRET`、存储根。
- 未上线的影响：prod 新生成图走回退（存远端 URL），功能可用但**不符合存储收口目标**。
- 验收：`prod:6008` 探活通过；用 ai-service 的 `INTERNAL_API_KEY` 调 `/internal/uploads/store` 返回 `code=0`；ai-service `.env` 补 `UPLOAD_SERVICE_URL` 后重启。

### A2 · nginx / 边缘机主机登记（P1）

- 机器：**42.194.200.69**（`dev.kedouai.com` 解析到这里；注意**不是** LIGHTHOUSE 101.43.117.234）。
- 缺：SSH 用户 / 密钥 / 部署根目录。
- 验收：控制台主机列表可见且可连通。

### A3 · 迁移待定 3 条评审（P2）

| 迁移 | 情况 | 待定 |
|---|---|---|
| `0008_knowledge_tables` | dev/prod 均缺 knowledge 3 表 | 执行 or 记账跳过 |
| `0010_pipeline_task_states` | `deploy_pipeline_runs` 已随域拆分迁到 `web_system_deploy`，**该迁移过时** | 记账跳过 |
| `0012_music_recommend` | music 领域未上 | 记账跳过 or 延期 |

- ⚠️ 记账 = 以后不再执行，宁可少记不可错记；`apply-migrations.sh` 在记账为空时会把**全部**当待应用（含基线脚本），**切勿盲跑**。

### A4 · A5 上传迁移的 prod 侧核对（P2）

- dev 已执行（`servers/user-service/uploads` 已空，产物在统一根 `/data/web_system/uploads/`）。
- 待定：prod 是否同样跑 `node scripts/migrate-uploads.mjs`（默认 DRY_RUN）。

### A5 · `deploy_env_service_routes`（P3）

- 本机只有 staging 遗留行；dev/prod 是否需登记待确认。

---

## B. 可推进 —— 无需外部输入

### B1 · C1 剩余 7 个服务接入统一认证助手（P1）

- 已合：PR #143，`packages/shared/src/auth/unified-auth.ts`，`todo-service` 首个接入。
- 剩余：user-service / ai-agent / ai-service / knowledge-service / system-service（`AuthMode.remote`）；gateway（本地 JwtService）/ upload-service（手写 HMAC）属 `AuthMode.local`，需注入各自校验实现。
- 验收：每个服务接入后行为不变 + 单测绿。

### B2 · A7 删 user-service 上传端点与 static serve（P2）

- 前置：A5 的 prod 侧验证通过。
- 约束：bianbian 特例路由**保留**作只读兜底。

### B3 · 回收 dev 上的历史产物暂存（P3）

- `/tmp/modules-legacy-20260924/`（108M）。
- 前置：观察期结束；全量备份 `~/backups/static-modules-20260924-0320.tgz` 已存在。
- 命令：`rm -rf /tmp/modules-legacy-20260924`（dev 机器上）。

### B4 · ai-service `uploads/` 残留（P3）

- `/data/web_system/servers/ai-service/uploads` 仍有 4.0K；确认是否并入统一根。

---

## C. 已完成（归档，不再跟进）

| 事项 | 落点 |
|---|---|
| A6：dev admin 指针 → `3d5ce61` | 控制台 switch API；入口与 manifest 已验证 |
| dev portal 指针 → `7a6be04` | 同上（原指向未投递的 `b2b6d4a`，入口 404） |
| dev/prod 共用库处置 | 不拆库 + 5 条护栏（见 `session-handover-2026-09-23.md` §8.8） |
| dev 历史产物清理 | `modules/` 119M → 11M |
| A8：ai-service 生成图改调 `internal/uploads/store` | PR #148；dev 验证通过（评审 `docs/reviews/a8-ai-image-store-release.md`，阻塞 0） |

---

## D. 已决策（不再讨论）

1. **dev 上 LEGACY 回退路径失效——接受**，不为它保留 `default/` 等旧目录。
   → 约束：**不要把 dev 的 gateway 切回 `DEPLOY_LEGACY_READ=1`**（会 404）。需要回退时从 tgz 恢复或重新发版。
2. **dev/prod 共用业务库——不拆库**，改用护栏；拆库排到正式运营前。
