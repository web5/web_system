# 技术架构文档

> Web System 全栈应用系统 — 架构设计、模块划分、路由规划与部署方案

---

## 目录

- [1. 项目概览](#1-项目概览)
- [2. 整体架构](#2-整体架构)
  - [2.1 开发环境拓扑](#21-开发环境拓扑)
  - [2.2 生产环境拓扑](#22-生产环境拓扑)
- [3. 技术栈](#3-技术栈)
- [4. 模块划分](#4-模块划分)
- [5. 网关设计](#5-网关设计)
- [6. 后端微服务](#6-后端微服务)
- [7. 前端应用](#7-前端应用)
- [8. 端口分配](#8-端口分配)
- [9. 部署架构](#9-部署架构)
- [10. 配置管理](#10-配置管理)
- [11. MCP 平台与鉴权](#11-mcp-平台与鉴权)

---

## 1. 项目概览

**Web System** 是一个全栈 Web 应用平台，包含管理后台、少儿教育门户、微信小程序、配套后端微服务，以及 **MCP 平台**（把后台服务零代码接入 MCP 协议供 AI 客户端调用）。项目采用 **pnpm workspace monorepo** 管理多应用、多服务的代码和依赖。

```
web_system/
├── apps/                      # 前端应用
│   ├── admin-web/            # 管理后台 (Vue3)
│   ├── portal/               # 少儿教育门户 (Vue3)
│   ├── mcp-admin/            # MCP 平台管理界面 (Vue3)
│   └── mini-app/             # 微信小程序 (UniApp)
├── servers/                   # 后端服务（9 个）
│   ├── gateway/              # API 网关 (NestJS，:6000)
│   ├── auth-service/         # 认证服务 (NestJS，:6001)
│   ├── user-service/         # 用户服务 (NestJS，:6002)
│   ├── ai-service/           # AI 服务 (NestJS，:6003)
│   ├── system-service/       # 系统服务 (NestJS，:6004)
│   ├── todo-service/         # 待办服务 (NestJS，:6005)
│   ├── mcp-gateway/          # MCP 工具网关 (NestJS，:6006)
│   ├── finnews/              # 财经资讯微服务 (NestJS，:6007)
│   └── upload-service/       # 上传服务 (NestJS，:6008)
├── packages/                  # 共享包
│   ├── types/                # TypeScript 类型定义
│   ├── shared/               # 共享工具函数与常量
│   ├── mcp-core/             # MCP 核心框架（声明式 HTTP 适配器）
│   └── ui/                   # 公共 UI 规范（设计 token + AntD 注册）
├── ecosystem.config.js        # PM2 进程管理配置
└── docs/                      # 技术文档
```

---

## 2. 整体架构

### 2.1 开发环境拓扑

```
   Browser ──────────┐
   portal (:5173) ───┤
   admin-web (:5174)─┤  Vite Dev Server（HMR）
   mcp-admin (:5175)─┘
                     │
                     ▼
              ┌──────────────────────┐
              │   Gateway (:6000)    │
              │  /health  → 心跳      │
              │  /api/*   → 代理后端  │
              │  /mcp-admin/ 静态托管  │
              └───┬───┬───┬───┬───┬──┘
                  │   │   │   │   │
        ┌─────────┘   │   │   │   └─────────┐
        ▼             ▼   ▼   ▼             ▼
     auth:6001    user:6002  ai:6003   ...   finnews:6007
                                       mcp-gateway:6006
```

### 2.2 生产环境拓扑

**三服务器架构**：

```
用户浏览器 / WorkBuddy
      │  HTTPS
      ▼
┌────────────────────────────────┐
│ SSL 层 42.194.200.69            │
│ Nginx — SSL 终止 / 域名路由      │
│ dev.kedouai.com / admin / portal│
│ /mcp 直连 mcp-gateway           │
└───────────────┬────────────────┘
                │  HTTP（安全组放行 22/80/6000-6008）
                ▼
┌────────────────────────────────┐
│ 应用层（DEV 175.27.189.123 或   │
│       PROD 106.52.176.246）     │
│                                │
│  Gateway (:6000)               │
│   ├─ /api/* → 各微服务          │
│   ├─ 静态托管 public/{portal,   │
│   │        admin,mcp-admin}     │
│   └─ /api/mcp、/api/finnews 代理│
│                                │
│  微服务 (:6001~6008)            │
│  MySQL (:3306)  Redis (:6379)   │
└────────────────────────────────┘
```

---

## 3. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | Vue 3 + TypeScript | SPA 应用 |
| **UI 库** | Ant Design Vue 4 | 企业级组件库 |
| **构建工具** | Vite | 快速开发与构建 |
| **状态管理** | Pinia | Vue 3 官方推荐 |
| **路由** | Vue Router | SPA 路由 |
| **后端框架** | NestJS | 企业级 Node.js 框架 |
| **HTTP 服务器** | Express (via NestJS) | 底层 HTTP 处理 |
| **API 代理** | http-proxy-middleware | 请求转发中间件 |
| **MCP 协议** | @modelcontextprotocol/sdk | streamable-http 端点 |
| **数据库** | MySQL + TypeORM | 生产环境持久化 |
| **缓存** | Redis | 会话/令牌缓存 |
| **认证** | JWT | 无状态认证 |
| **包管理** | pnpm workspace | Monorepo 依赖与构建管理 |
| **进程管理** | PM2 | 生产环境进程守护 |
| **反向代理** | Nginx | SSL 终止、域名路由、静态资源 |
| **微信小程序** | UniApp | 跨平台小程序框架 |

---

## 4. 模块划分

### 4.1 apps/ — 前端应用

| 模块 | 说明 | 技术 | 开发端口 | 生产托管 |
|------|------|------|---------|---------|
| `admin-web` | 管理后台 | Vue3 + AntD + Vite | 5174 | gateway `public/admin/` |
| `portal` | 少儿教育门户 | Vue3 + AntD + Vite | 5173 | gateway `public/portal/` |
| `mcp-admin` | MCP 平台管理界面 | Vue3 + AntD + Vite | 5175 | gateway `public/mcp-admin/` |
| `mini-app` | 微信小程序 | UniApp | — | — |

### 4.2 servers/ — 后端微服务（9 个）

| 模块 | 说明 | 端口 |
|------|------|------|
| `gateway` | API 网关：统一入口 + 代理 + 静态托管 | 6000 |
| `auth-service` | 认证服务：JWT 签发/验证 | 6001 |
| `user-service` | 用户服务 | 6002 |
| `ai-service` | AI 服务：对话/生图/变变 | 6003 |
| `system-service` | 系统服务 | 6004 |
| `todo-service` | 待办服务 | 6005 |
| `mcp-gateway` | MCP 工具网关：声明式 HTTP → MCP 工具 | 6006 |
| `finnews` | 财经资讯微服务 | 6007 |
| `upload-service` | 上传服务 | 6008 |

### 4.3 packages/ — 共享包

| 模块 | 说明 |
|------|------|
| `@web-system/types` | 共享 TypeScript 类型定义 |
| `@web-system/shared` | 共享工具函数与常量（API_TIMEOUT 等） |
| `@web-system/mcp-core` | MCP 核心框架：buildServer / createHttpModule / 声明式 HTTP 适配器 |
| `@web-system/ui` | 公共 UI 规范：设计 token + AntD 组件注册清单 |

---

## 5. 网关设计

Gateway（:6000）是整个系统的统一入口，负责多类请求分发：

| 分类 | 前缀 | 职责 |
|------|------|------|
| 🔍 健康检查 | `/health` | 服务存活性检测 |
| 🔄 API 代理 | `/api/*` | 转发到各微服务 |
| 🔀 MCP 代理 | `/api/mcp/*`、`/api/finnews/*` | 转发到 mcp-gateway / finnews（含鉴权） |
| 📦 静态资源 | `/assets/*`、`/portal/`、`/admin/`、`/mcp-admin/` | SPA 前端文件托管 |
| 🌐 SPA 回退 | 无后缀 GET | 返回对应 `index.html` |
| 📚 接口文档 | `/docs`、`/swagger` | Swagger 文档 |

完整的路由映射、代理规则、路径重写见独立文档：

> 📄 [网关 URL 规划](./网关URL规划.md)

---

## 6. 后端微服务

### 6.1 Auth Service — :6001

- 用户注册/登录、微信 OAuth、小程序登录
- JWT Token 签发与验证
- 依赖：MySQL、JWT Secret

### 6.2 User Service — :6002

- 用户 CRUD、状态管理
- 依赖：MySQL

### 6.3 AI Service — :6003

- AI 对话、生图、变变（bianbian）
- 依赖：外部 AI API

### 6.4 System / Todo / Upload — :6004 / :6005 / :6008

- 系统配置、待办事项、文件上传
- 依赖：MySQL

### 6.5 MCP Gateway — :6006

- MCP streamable-http 端点 `/mcp`、`/mcp/:module`
- 声明式 HTTP 适配器：把后台 REST 接口转成 MCP 工具
- 模块管理 API `/api/modules`（供 mcp-admin 调用）
- 依赖：MySQL（模块/工具表）、`@web-system/mcp-core`

### 6.6 Finnews — :6007

- 财经资讯采集（新浪/东财快讯）、SimHash 去重、LLM 摘要/情感分析
- 对外提供 5 个 REST 接口：`/api/topics`、`/api/search`、`/api/stock-news`、`/api/sector-hot`、`/api/market-pulse`
- 依赖：MySQL、LLM API

---

## 7. 前端应用

### 7.1 admin-web（管理后台）— 5174

- 功能：用户管理、工作台、系统设置
- 生产托管：gateway `public/admin/`，访问 `/admin/`

### 7.2 portal（少儿教育门户）— 5173

- 功能：首页、画笔页面（Canvas 画板）
- 生产托管：gateway `public/portal/`，访问 `/portal/`

### 7.3 mcp-admin（MCP 平台管理界面）— 5175

- 功能：模块注册/编辑/删除/启停、工具调试验证
- 生产托管：gateway `public/mcp-admin/`，访问 `/mcp-admin/`

### 7.4 mini-app（微信小程序）

- 框架：UniApp
- 功能：首页、画板工具

---

## 8. 端口分配

| 服务 | 开发端口 | 生产端口 | 说明 |
|------|---------|----------|------|
| Gateway | 6000 | 6000 | API 网关 |
| Auth Service | 6001 | 6001 | 认证服务 |
| User Service | 6002 | 6002 | 用户服务 |
| AI Service | 6003 | 6003 | AI 服务 |
| System Service | 6004 | 6004 | 系统服务 |
| Todo Service | 6005 | 6005 | 待办服务 |
| MCP Gateway | 6006 | 6006 | MCP 工具网关 |
| Finnews | 6007 | 6007 | 财经资讯 |
| Upload Service | 6008 | 6008 | 上传服务 |
| Portal (Vite) | 5173 | — | 门户开发服务器 |
| Admin Web (Vite) | 5174 | — | 后台开发服务器 |
| MCP Admin (Vite) | 5175 | — | MCP 管理界面开发服务器 |
| MySQL | — | 3306 | 数据库 |
| Redis | — | 6379 | 缓存 |
| Nginx (应用层) | — | 80 | 静态资源 + 反向代理 |
| Nginx (SSL 层) | — | 443 | SSL 终止 + 域名路由 |

> 后端端口统一 600x（2026-08 从 300x 迁移），前端保持 517x。

---

## 9. 部署架构

### 9.1 生产部署（三服务器）

| 服务器 | IP | 角色 |
|--------|-----|------|
| SSL 层 | 42.194.200.69 | Nginx：SSL 终止、域名路由、`/mcp` 直连 mcp-gateway |
| 应用层 DEV | 175.27.189.123 | Gateway + 9 微服务 + MySQL + Redis |
| 应用层 PROD | 106.52.176.246 | 同 DEV（生产环境） |

**请求链路：**

```
用户/WorkBuddy → HTTPS → SSL 层 Nginx → 应用层 Gateway/静态资源 → 微服务
```

**进程管理**：PM2 管理 9 个微服务进程，配置见 `ecosystem.config.js`。

**部署流程**：

1. 本地构建：各服务 `pnpm build` + 前端 `pnpm build`
2. 上传产物：`scp` dist 到应用层服务器
3. 重启服务：`pm2 start ecosystem.config.js --only <service>`
4. 前端部署：`./scripts/deploy.sh dev <portal|admin|mcp-admin>`

### 9.2 本地开发

各服务独立 `pnpm dev` 启动，端口见「8. 端口分配」。前端用 Vite HMR。

---

## 10. 配置管理

### 10.1 环境变量

- 生产：`.env.production`（DB、JWT、微信、服务间密钥 `FINNEWS_SERVICE_KEY` 等）
- 开发：各服务 `servers/*/.env`
- PM2 通过 `ecosystem.config.js` 的 `process.env` 注入环境变量

### 10.2 PM2 配置

文件：`ecosystem.config.js`

管理 9 个微服务进程，配置日志轮转、内存限制、自动重启、环境变量注入。

---

## 11. MCP 平台与鉴权

> MCP 平台是 2026-08 新增的子系统：把后台微服务零代码接入 MCP 协议，供 WorkBuddy 等 AI 客户端调用。

### 11.1 组件

| 组件 | 端口 | 说明 |
|------|------|------|
| `mcp-gateway` | 6006 | MCP 工具网关：声明式 HTTP 适配器转 MCP 工具，暴露 `/mcp`、`/mcp/:module` |
| `finnews` | 6007 | 财经资讯微服务（第一个接入，code_key=`finnews`） |
| `mcp-admin` | — | 管理界面，托管 gateway `public/mcp-admin/` |
| `@web-system/mcp-core` | — | 共享包：buildServer / createHttpModule / HTTP 适配器 |

### 11.2 关键链路

```
WorkBuddy → https://dev.kedouai.com/mcp/finnews → mcp-gateway(:6006)
  → 声明式 HTTP 模块 → https://dev.kedouai.com/api/finnews/* → gateway(:6000)
  → 鉴权 → finnews(:6007)
```

- `/mcp` 聚合所有启用模块工具；`/mcp/:module` 按 `code_key` 只暴露单模块
- 服务间鉴权（mcp-gateway ↔ gateway）详见独立文档

> 📄 [MCP 服务间鉴权方案](./MCP服务间鉴权.md)

---

> 文档版本：v2.0
> 更新时间：2026-08-14
> 变更说明：端口 300x→600x、服务扩到 9 个、新增 MCP 平台、三服务器拓扑、pnpm workspace
> 关联文档：[网关 URL 规划](./网关URL规划.md) · [MCP 服务间鉴权](./MCP服务间鉴权.md)
