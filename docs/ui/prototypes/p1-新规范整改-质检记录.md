# P1 样板批 · 新规范整改质检记录（admin-settings / version-deploy）

> 日期：2026-09-23 ｜ 批次：整改清单 P1（`specs/ui-prototype-token-alignment/整改影响清单.md`）
> 范围：`docs/ui/prototypes/admin-settings.html`、`docs/ui/prototypes/deploy-console-version-deploy.html`
> 性质：**批量机械整改**（不加功能、不改信息架构），按 ui-interface 第 9 条先出清单后执行。

## 1. 设计决策（改了什么、为什么）

| 条目 | 决策 |
|---|---|
| E10 正文/强调分离 | 新增 `--ws-text-body`（正文）与 `--ws-text-strong`（标题/强调）；长文正文与表格正文改引 body，标题/按钮文字改引 strong |
| E11 行高/字距 | 字面 `line-height` 全归 `--ws-leading-relaxed`（1.75）；标题（h1/h2/h3、小标题）加 `letter-spacing: var(--ws-tracking-tight)` |
| E12 字阶与例外 | 阶外字号就近归阶（11/11.5→12、12.5/13.5→13、15→16、19/22→24/18）；**保留的例外均写注释登记**（代码块 12.5、图标按钮 line-height:1、外壳圆角、几何圆形 50%） |
| E13 控件/图标 | 图标尺寸归 `--ws-icon-inline/feature`；radio/spin 圆点归 icon 档 |
| G7 圆角 | 字面圆角归 `--ws-radius-lg/md/pill`；新增 pill 变量；外壳 14/16px 与圆形 50% 登记为例外 |
| G11 焦点态 | 两份均加 `:focus-visible{box-shadow:var(--ws-focus-ring)}`，鼠标点击不显 ring |
| G12 动效 | 全部 `transition` 时长归 `--ws-duration-fast/base` + `--ws-ease-out`（150/200ms） |
| E3 字重 | `version-deploy` logo 原 `font-weight:700` → 600（规范只用 400/500/600） |
| 漂移修正 | `version-deploy` 自建 token 与官方值漂移已对齐：`--text #1A1F2E→#3F3F46`、`--radius 14/10 → 8/6` |

## 2. 逐条质检（ux-review-checklist A~I）

| 维度 | 结论 | 说明 |
|---|---|---|
| **A 信息架构** | ✅ 不受影响 | 整改不动结构与导航，页面/入口数量不变 |
| **B 任务流闭环** | ✅ 不受影响 | 交互逻辑未改；实跑确认 tab 切换、抽屉、按钮仍可用 |
| **C 状态矩阵** | ✅ 不受影响 | 加载/空态/失败/例外屏均保留（`st-exception`、`drawer-empty/err` 等） |
| **D 操作与确认** | ✅ 不受影响 | 无新增/删除操作 |
| **E 视觉与 Token** | ✅ 达标 | 业务样式裸 hex：admin-settings 2→0；version-deploy 5→0；字面行高基本清零（仅 1 处图标按钮登记例外）；字号全部落入字阶 |
| **F 可访问性基线** | ✅ 改善 | 两份新增 `:focus-visible` ring（整改前均无）；正文对比度随 text-body 仍过 AA |
| **G 内容与真实性** | ✅ 不受影响 | 未改文案，无 lorem/占位新增 |
| **H 目标端一致性** | ✅ 合规 | admin 系仅用 `--ws-*`；未混入品牌端 token；桌面壳仍为桌面形态 |
| **I 未定义项标注** | ✅ 已登记 | 4 类例外均写注释并汇总于本文 §3 |

## 3. 例外登记（E12/G7 口径，落码时同样适用）

| 例外 | 位置 | 理由 |
|---|---|---|
| 外壳圆角 14px / 16px | `.ad-desktop` / `.desktop` | 模拟桌面窗口，非产品 UI 圆角档 |
| 几何圆形 `50%` | 头像、开关滑块、radio、spin | 圆形是形状而非圆角档 |
| 图标按钮 `line-height:1` | `.drawer-close` | 图标垂直居中需要，非正文行高 |
| 代码块 12.5px | 对话区 code 块（品牌端同） | 等宽场景惯例，已在 `specs/agent-message-extract/page-spec.md` 登记 |

## 4. 浏览器实跑证据（2026-09-23，8900 静态服务）

| 文件 | URL | Console | 关键断言 |
|---|---|---|---|
| admin-settings.html | `/docs/ui/prototypes/admin-settings.html` | 1 error（favicon 404，无关） | ✓ 页面标题渲染 ✓ Tab 结构存在 |
| deploy-console-version-deploy.html | `/docs/ui/prototypes/deploy-console-version-deploy.html` | 0 error | ✓ 部署内容渲染 ✓ 抽屉/发布元素存在 |

## 5. 视觉变化点（需人眼验收，与整改前对比）

整改是"就近归拢"，**视觉会有变化**，以下为有意变化、非回归：

- **正文更柔和**：近黑 → 深灰（admin `#171717→#3F3F46`；version-deploy `#1A1F2E→#3F3F46`）
- **圆角更克制**（Geist 语言）：admin 卡片 14→8；version-deploy 卡片 14→8、元素 10→6
- **字号归阶**：11/11.5→12、12.5/13.5→13、15→16、19→18、22→24、20→18
- **空态图标变小**（admin）：44→24（对齐 `icon.feature`）
- **动效统一**：各处 `.15s/.2s/.22s` → 150/200ms ease-out

## 6. 遗留 / 下一步

- P1 完成，待负责人按 §5 验收。
- **P2 开工前需确认**：品牌端（music-taste / 小程序）用 **rpx**，阶梯是否按 `1px≈2rpx` 映射，或单独定义 rpx 阶梯（清单 §6 待确认 3）。
- P3（domain-split 275 处裸 hex）按屏分批，周期长，另行排期。
