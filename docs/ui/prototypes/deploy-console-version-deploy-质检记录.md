# 原型质检记录 · deploy-console-version-deploy.html v1

> 开关：CHANGELOG=off · HISTORY_NOTE=off · FAQ_KEEP=on
> 日期：2026-09-21 ｜ 目标端：桌面 Web（deploy-console 管理台）

## A. 信息架构
- [x] 页面清单完整：版本部署主屏（微前端 / API 网关两个域入口共用）、应用详情入口改造示意屏、部署抽屉、确认弹窗、说明弹窗。
- [x] 入口可达 + 返回路径：侧边栏域入口 → 主屏；应用详情屏有面包屑返回；抽屉/弹窗均可关闭。
- [x] 层级 ≤3（域 → 部署页 → 抽屉/确认），无跳转环。

## B. 任务流闭环
- [x] 主路径闭环：选环境 tabs → 行「部署」→ 抽屉选版本 → 确认（含后果文案）→ loading → toast + 表格当前版本更新（行高亮）。
- [x] 异常分支：空态（local@mcp-admin，原因+出路=去发布流水线）、错误态（local@portal，重试可恢复）、重试成功路径可演示。
- [x] 跨页衔接：应用详情「部署」打开同一抽屉（与独立部署页同交互，不跳流水线）。

## C. 状态矩阵
- [x] 加载中：抽屉打开有 500ms 版本加载态；部署按钮 spinner。
- [x] 空/错态可见且有唯一恢复入口。
- [x] 完成态：toast + 当前版本列刷新 + 行高亮。
- [x] 禁用态：未选版本时部署按钮 disabled + 文案说明；当前版本 radio 置灰不可选。

## D. 操作与确认
- [x] 主操作一屏 ≤1（primary 仅抽屉底部「部署到环境」+ 弹窗「确认部署」，不同层级不并存）。
- [x] 破坏性确认：后端服务确认弹窗写明「替换产物目录并重启 pm2，中断约 3~5 秒」；前端切指针也经二次确认（影响线上加载版本）。
- [x] 重复提交锁：部署中按钮 disabled + spinner。

## E. 视觉与 Token 一致性
- [x] 裸 hex 仅在 `:root`；语义色经 `--danger/--warn/--ok` 表达（新增 `--info` 信息色变量，随 `:root` 定义，符合「新色先入变量」精神；落地时需同步进 packages/ui tokens，已列入落地注意项）。
- [x] admin 主橙 #F97316（DR-3）；亮底单稿。
- [x] 无 emoji 图标，全部内联 SVG；间距 8 倍数。
- [x] 版本 tag / commit / 路径用 mono；层级靠字号+文字三级色。

## F. 可访问性基线
- [x] 文字对比度达标（text/text-2/text-3 对 card）；字号 ≥11.5px。
- [ ] 键盘焦点：原型阶段未实现 Tab 焦点顺序（标注：落地时 antd 组件自带，不另行处理）。

## G. 内容与真实性
- [x] 真实模块名（admin/portal/mcp-admin、auth/system/todo）、真实字段语义（versionTag/branch/commit/发布人/时间）、真实导航域结构（对齐 MainLayout 双域）。

## H. 目标端一致性
- [x] 目标端：桌面 Web（管理台场景明确，无需用户拍板）；桌面壳无跨端元素混入。
- [x] 无平台原生能力伪装。

## I. 未定义项显式标注
- [x] 待确认项三条已在「本稿说明」弹窗与右侧评审注释中显式列出：
  ① 导航归属（双域入口 vs 独立一级域）；② 应用详情「部署」入口是否一并改；③ 后端服务「选版本直接部署」是否开放（涉及重启）。
- 注：产物存在性守卫（版本目录被 cleanup 清掉）用户已明确「先不做」，原型未画该分支。

## 结论
通过（遗留：F 键盘焦点为落地阶段事项；`--info` 变量落地时需入 tokens.ts/tokens.css 双文件）。
