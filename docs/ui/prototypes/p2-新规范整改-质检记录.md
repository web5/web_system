# P2 中等批 · 新规范整改质检记录（pipeline-canvas / music-taste）

> 日期：2026-09-23 ｜ 批次：整改清单 P2（`specs/ui-prototype-token-alignment/整改影响清单.md`）
> 范围：`docs/ui/prototypes/pipeline-env-branch-canvas.html`（admin 系 px）、`apps/kedou-ai-minigram/prototype/music-taste.html`（品牌端，落地按 1px≈2rpx）
> 前置决策：**品牌端阶梯按 1px ≈ 2rpx 映射**（负责人 2026-09-23 确认，已记入清单 §7）

## 1. 设计决策

| 条目 | 决策 |
|---|---|
| E10 | 两份均引入 `--text-body`（正文）/`--text-strong`（标题强调）；body 默认色与输入文字改 body，标题改 strong |
| E11 | 字面 `line-height`（1.6/1.7/1.8/1.9/2）归 `leading-normal(1.6)` / `leading-relaxed(1.75)`；标题加 `tracking-tight` |
| E12 | 阶外字号归阶：9/10/10.5/11/11.5→12、12.5→13、15→16、17→18、19→18、22→24；保留例外写注释（代码块 12.5、原生电池图标圆角、图标按钮行高） |
| E13 | 图标尺寸归 `icon-inline/feature`；控件高度归 `control-*`（如 composer 输入 40→`control-lg`） |
| G7 | 字面圆角归 `--radius / -sm / -xs / -pill`（新增 xs 与 pill 档）；外壳 16px 与几何圆形 50% 登记例外 |
| G11 | 两份均加 `:focus-visible` ring（整改前均无） |
| G12 | 全部 `transition` 归 `duration-fast/base/slow` + `ease-out`，移除自定义 cubic-bezier |
| E3 | `font-weight:700` → 600（pipeline 5 处） |
| 漂移修正 | pipeline 自建 token 对齐官方：`--text #1A1F2E→#3F3F46`、`--radius 14/10 → 8/6`；并新增**深壳语义 token**（`--dc-top-bg` / `--dc-sider-bg` / `--on-dark*` / `--stage-bg`）消化 15 处裸 hex |
| 品牌端单位 | music-taste 的 :root 阶梯变量附 **rpx 换算注释**（1px≈2rpx），落码照此换算 |

## 2. 逐条质检（ux-review-checklist A~I）

| 维度 | 结论 | 说明 |
|---|---|---|
| A 信息架构 | ✅ 不受影响 | 不改结构与导航 |
| B 任务流闭环 | ✅ 不受影响 | pipeline 画布交互、music-taste 壳内切换均未动 |
| C 状态矩阵 | ✅ 不受影响 | 空态/骨架/失败/抽屉等状态屏保留 |
| D 操作与确认 | ✅ 不受影响 | 无新增/删除操作 |
| E 视觉与 Token | ✅ 达标 | pipeline：业务裸 hex 15→**0**，字面行高 6→**0**，非 token 时长 3→**0**，字号全在阶；music-taste：字面行高 3→**0**，非 token 时长 6→**0**，字号全在阶（阶外 23 处已归） |
| F 可访问性 | ✅ 改善 | 两份新增 `:focus-visible`（整改前均缺）；极小字号（9/10.5px）归到 12px 提升可读性 |
| G 内容真实 | ✅ 不受影响 | 文案未改 |
| H 目标端一致 | ✅ 合规 | pipeline 仅 `--ws-*` + 深壳 `--dc-*`（对齐落地 MainLayout）；music-taste 仅品牌端 token，未混 admin 变量 |
| I 未定义标注 | ✅ 已登记 | 例外汇总见 §3 |

## 3. 例外登记（落码同样适用）

| 例外 | 位置 | 理由 |
|---|---|---|
| 外壳圆角 16px | pipeline `.desktop` | 模拟桌面窗口，非产品圆角档 |
| 几何圆形 `50%` | 头像、删除钮、新增节点、胶囊圆点 | 圆形是形状而非圆角档 |
| 原生电池图标 3.5 / 1.5 / 1px | music-taste `.wxbar .bat` 系列 | 模拟微信原生状态栏细节 |
| 代码块 12.5px | 对话区（品牌端同口径） | 等宽场景惯例，已在 `specs/agent-message-extract/page-spec.md` 登记 |
| `border-radius:0 0 …` / `… 0 0` | 局部圆角写法 | 0 为合法值，非圆角档 |

## 4. 浏览器实跑证据（2026-09-23，8900 静态服务）

| 文件 | Console | 关键断言 |
|---|---|---|
| pipeline-env-branch-canvas.html | 0 error | ✓ 流水线/步骤/任务画布内容渲染 |
| music-taste.html | 0 error | ✓ 歌曲推荐卡片 / 音乐口味内容渲染 |

## 5. 视觉变化点（需人眼验收）

- **pipeline**：正文近黑→深灰；圆角普遍变小（14→8、10→6、6→4 档位重排）；深壳色改为语义 token（视觉值不变）；字号 9/10/11→12、15→16、19→18；字重 700→600
- **music-taste**：正文→深灰；圆角归档（8→sm、14/18→radius、50px→pill）；字号 10.5/11/11.5→12、12.5→13、15→16、17→18；动效统一 150/200/300ms

## 6. 遗留

- P2 完成，待验收。
- **结构漂移待决**（清单 §6）：`version-deploy` 与 `pipeline-canvas` 两份的壳均为**旧单级侧栏**，落地 `MainLayout.vue` 是「顶部一级 + 左侧二级」——是否换壳待负责人定（A 现在改 / B 排队）。
- 下一步 P3：`deploy-console-domain-split.html`（275 处裸 hex）按屏分批，另行排期。
