# 交互质检记录 · 流水线产品逻辑 v13 原型

> 原型：`docs/ui/prototypes/release-platform-v13-pipeline-product-logic.html`
> 规格：`docs/ui/page-specs/pipeline-product-logic-v1.md`
> 清单：`ux-prototype-designer/references/ux-review-checklist.md`
> 结论：**通过（3 项遗留，已标注）** —— 通过后仍需用户拍板才进实现

## A. 信息架构
- [x] 页面完整：流水线列表 / 流水线变量 / 编辑流水线 / 流水线详情，四屏齐备
- [x] 边界正确：配置中心以「不可点 + 灰字」标注为**服务配置、与流水线无关**，本稿不改动它（用户 2026-09-14 澄清）
- [x] 每个入口可达、可返回：左侧导航 + 抽屉/弹窗均有取消与遮罩关闭
- [x] 层级 ≤2（导航 → 内容 → 抽屉），无跳转环
- ⚠️ 遗留：抽屉内「编辑已有节点」仅做新增演示，未回显既有值（实现时必做）

## B. 任务流闭环
- [x] 主路径：新建模板 → 节点序列预览 → 进编辑 → 新增审批节点/变量 → 保存
- [x] 异常：节点 key 非法 / 与保留字冲突 → 字段级错误文案；变量键为空 → Toast
- [x] 跨页衔接：编辑页的变量 → 详情页「参数」面板可见（PUBLISH_HOST/PATH 标注来源=模板）

## C. 状态矩阵
- [x] 加载中：保存按钮 disabled +「保存中…」
- [x] 空态：变量表 / 节点列表均有空态 + 唯一恢复入口（新增按钮）
- [x] 完成态：Toast 反馈
- ⚠️ 遗留：未演示「审批人拉取失败（degraded）」的界面态，仅在审批字段说明里写了告警文案（实现时按 UserSelect 组件自带告警呈现）

## D. 操作与确认
- [x] 主操作一屏 ≤1（编辑页仅「保存」为 primary）
- [x] 破坏性：删除节点为图标按钮，实现时按规格加 Modal 二次确认（原型未展开，规格已列）
- [x] 重复提交锁：保存按钮置灰
- [x] 字段级校验文案

## E. 视觉与 Token
- [x] 无裸 hex（除 `:root`）；语义色走 --danger/--warn/--ok
- [x] 主色 admin 平台橙 --primary #F97316（DR-3）
- [x] 无 emoji；图标用文字/符号（⚙ ✕ ↑ ↓ 为原型简化，实现替换为 antd 图标）
- [x] 间距 8 的倍数；字号三级（18/13/12）

## F. 可访问性基线
- [x] 对比度达标、正文 ≥12px
- ⚠️ 遗留：键盘焦点未做（原型阶段标注即可，实现用 antd 组件自带）
- [x] 可点击元素均有 cursor/hover 反馈

## G. 内容与真实性
- [x] 文案为真实业务文案（PUBLISH_PATH / 发布确认 / 切指针…）
- [x] 关键字段完整：审批人、来源、被覆盖、超时、拒绝动作

## H. 目标端一致性
- [x] 目标端 = 桌面 Web（已确认并回显）
- [x] 用项目 `.desktop` 壳（侧边栏 + 顶栏 + 内容区），无移动端元素混入
- [x] 无平台原生能力伪造

## I. 未定义项显式标注
- [x] 已在设计决策记录中列出 3 项待确认（见下）

---

## 设计决策记录（本稿的取舍与待确认）

| # | 事项 | 本稿处理 | 状态 |
|---|---|---|---|
| 1 | 审批人候选拉取失败（degraded） | 组件内告警文案说明；原型未单独做该界面态 | 待确认：是否需要在原型里补一个降级态演示 |
| 2 | `onReject=rollback` | 原型可选（用户已确认本期实现），提示「回滚到基线版本」 | 已定：实现，基线在提交时快照 |
| 3 | 编辑已有节点的回显 | 原型只演示新增 | 待确认：实现时按节点类型回显全部字段 |
| 4 | 变量「密钥」 | 列表掩码显示 **** ，不可回显 | 已定 |
| 5 | admin / ai-agent 发布路径不同，全局变量共用怎么办 | **方案 B —— 做「流水线级」变量**：变量挂在**某一条流水线**上，可覆盖同名全局键；脚本统一引用 `${PUBLISH_PATH}` | **已拍板（2026-09-14）**：B；但**不是模板级** —— 用户澄清实体只有「流水线 + 流水线实例」两层（见 #7），因此第二级作用域是**流水线级**（`scope=pipeline`，外键指向流水线 id），不存在「模板级」 |
| 6 | 侧边栏菜单归属（用户批注） | 4 个入口收进父菜单 **「流水线管理」**（可折叠子菜单，默认展开、点父项收起；跳转时自动展开并高亮） | 已定；实现时 `deploy-console` 的 `MainLayout.vue` 菜单同步调整 |
| 7 | 实体口径：「流水线 / 流水线模板 / 流水线实例」是几个实体 | 只有**两个**：流水线（= 发布流程定义，当前落在 `deploy_pipeline_templates` 表 / `pipeline-templates` API，历史上叫「模板」，实体上就是流水线本身）+ 流水线实例（`deploy_pipelines`）。**不存在第三层「流水线模板」** | 已定；UI 与文档术语统一叫「流水线」，表名/API 的 `template` 字样是待清理的技术债务（不在本期改名） |
| 8 | 流水线尾巴太长：投递产物 + 写版本、切指针、验证 | **合并 + 外迁**：①投递产物 + 写版本合成一个「**发布**」节点（产物落地即写版本记录，是一个整体）；②**切指针 + 探活验证移出流水线**，归「模块管理 → 环境部署」 | 已定（2026-09-14）；流水线默认序列变为：`拉取代码 → 构建 → 发布确认(审批) → 发布` |
| 9 | 模块管理长什么样 | 三个子模块：**配置信息**（前端=名称/路由 path；后端=端口/启动命令/健康检查）、**版本列表**（流水线产出，前后端都可多版本，按版本分目录）、**环境部署**（按环境列表，选版本部署） | 已定；原型已出三个子页待确认；**注意：现有实现里指针/验证还在流水线内（`version` / `pointer` platform 节点），本次调整意味着这部分要挪窝** |
| 10 | 发布节点要不要内置「写版本」语义 | **不要**：发布就是普通 shell 节点，脚本两步 —— ①上传文件（工具）②调用写版本接口（工具），走统一技术流程，引擎不为它开后门 | 已定（2026-09-14）；编排页不再显示「含内置 写版本」标签，改为显示脚本动作 `上传文件 + 调用写版本接口` |
| 11 | 部署要不要自动探活验证 | **不要**：部署 = 调用改指针接口，基本不会失败；**本期人工验证**。后续接 AI 验证 agent 时加「AI 验证」按钮下发验证任务 | 已定；「验证」列显示「人工」，AI 验证按钮**放在版本列表的某一行**（用户 2026-09-14 指定） |
| 12 | 流水线列表默认长什么样 | **一屏列出所有模块的流水线**（含「模块」列），每个模块默认 3 条：local / dev / prod | 已定；原型列表页 6 行（admin 3 + gateway 2 + ai-agent 1），行内「编辑 / 详情」 |
| 17 | 模块详情版本行的「部署」怎么交互 | **弹窗选择**：从版本行点部署 → 弹窗里**版本固定、选目标环境**；从环境行点部署 → 弹窗里**环境固定、选版本** | 已定（2026-09-14）；不在行内就地部署 |
| 13 | 流水线变量要不要单独成页 | **不单独成页，跟着流水线走**：在「编辑流水线」页增删改查，在「流水线详情」用 Tab 查看（执行进度 / 参数 / 变量） | 已定；菜单里已移除「流水线变量」，详情新增「变量」Tab |
| 14 | 编辑流水线的编排交互 | **保持现有流程画布**：拖拽改顺序、连接线上的 ＋ 插入节点、节点上 ✕ 删除、点节点进抽屉改脚本 / 变量值 / 超时 / 审批人 | 已定；原型编排区已改为画布（节点卡 + 连接线 ＋，支持拖放排序与删除） |
| 15 | 模块管理的导航结构 | **列表 → 详情 → 编辑**：默认进列表，点记录进详情（三 Tab：配置信息 / 版本列表 / 环境部署，只读查看 + 版本直接部署 + 环境点部署选版本），点「编辑」进编辑页（同三 Tab，可改可保存） | 已定；原型已按此出 3 个屏幕 |
| 18 | v13 分稿要不要并入整体框架稿 | **并入**：以 v5 框架稿为底复制出 `release-platform-v14-整合框架.html`，把 v13 的流水线/模块页面整合进去（菜单改为「流水线管理 / 模块管理」两个可折叠父菜单；详情加参数+变量 Tab；模块详情/编辑三 Tab；编辑态加变量区），其余页面（仪表盘/环境/监控/审计/配置中心/通知/灰度/诊断/工具/设置）原样保留 | 已定（2026-09-14）；**主稿 = v14**，v13 分稿保留作历史 |
| 16 | 流水线详情的 Tab 里为什么是「参数」不是「历史记录」（用户批注） | 两者都要：详情 Tab = **执行进度 / 历史记录 / 参数 / 变量**。「历史记录」是实例的事件流（提交 / 节点开始结束含耗时与退出码 / 审批动作 / 状态流转），参数只是排查"改变量不生效"的辅助视图 | 已定（2026-09-14）；原型已加「历史记录」Tab（4 个 Tab） |

## 质检结论
通过（A/C/F 各 1 项遗留，均为原型阶段可标注项，不阻塞用户拍板）。

---

## J. 补充质检 · 流水线列表默认排序（2026-09-21）

> 范围：仅列表**默认排序**（Quick，规格见 `docs/ui/page-specs/pipeline-product-logic-v1.md` §10）；无视觉/控件改动，下列按 ux-review-checklist 取相关项复核。

- [x] 规则无歧义：主键 = 最近实例 `startTime` 降序（最新在上）；**无实例者统一置尾**
- [x] 稳定排序：时间相同 / 均无实例时保持既有「模块 × 流水线」顺序，避免刷新跳行
- [x] 与筛选协同：筛选后仍为倒序；分页 15 条不变，**不新增排序控件**
- [x] 空态：全部从未执行时顺序与既有默认一致，无需额外文案（「从未执行」灰字已有）
- [x] 无 Token / 颜色 / emoji 新增
- 结论：**通过**（无遗留项），待用户确认后落码
