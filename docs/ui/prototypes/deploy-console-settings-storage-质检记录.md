# 质检记录 · deploy-console「系统设置 / 存储配置」区块（A6）

> 原型：`docs/ui/prototypes/deploy-console-domain-split.html` → `dc-settings` 屏（2026-09-22 增量）
> 规格：`specs/backend-consolidation/page-spec-storage-config.md`
> 需求与后端契约：`specs/backend-consolidation/design.md` §1.2 / §1.4 / §1.5（议题 A6）
> 角色：`ux-prototype-designer`（原型 + 本记录）｜质检清单：`.codebuddy/skills/ux-prototype-designer/references/ux-review-checklist.md`

## 一、设计决策记录

| 项 | 结论 |
|---|---|
| **目标端** | **桌面 Web**（deploy-console 深色控制台壳，1280×820）。非多端，无需分区 |
| 对齐对象 | 信息架构 + 交互细节（**高保真**）：真实文案、真实路径样例、可点击跳转与模拟时序 |
| 本次范围 | ① 把「系统设置」从**占位 generic 屏**升级为真实屏（顺带修掉与代码的漂移）；② 新增「存储配置」区块 |
| 与落地对齐 | 「通知与审批」字段按 `apps/deploy-console/src/views/SystemSettings.vue` 现有内容还原（通用 Webhook / 企业微信 Webhook / 需审批环境） |
| 主操作 | 每个卡片各 1 个主操作用各自的保存按钮；「存储配置」的保存为 **primary**，「通知与审批」的保存为次要按钮（避免同屏 2 个 primary 打架） |
| 双值语义 | 「**当前生效目录**」= upload-service 启动时采纳的内存值（`GET /internal/storage/path` on 6008）；「**待生效目录**」= 权威配置值（`GET /api/admin/settings/storage`）。两者不同 → 徽标「待重启生效」+ warn 条，说清「重启生效、不做热切换」 |
| 选择方式 | **目录树为主 + 文本输入兜底**（design §1.4 已定）：树选择需 `storage:browse`（仅 super_admin）；非超管或 `storage.browse_enabled=0` 时树按钮**禁用并说明原因**，自动落回文本输入 |
| 校验前置 | 保存按钮**默认禁用**，需先「校验」通过（文案随状态变化）。越界 / 不可写 / 非目录 → 禁止保存；**「目录不存在」允许保存**（PUT 会自动 `mkdir -p`），文案显式说明 —— 与后端实现一致 |
| 目录树约束的可见化 | 只列目录不列文件、截断提示（前 200 项）、「解链后越界的符号链接会被跳过」写在树脚注，避免评审以为能当文件浏览器用 |
| 状态演示条 | 右上 `[原型] 状态演示`（9 态）**是原型工具，不是产品功能**：用于走查状态矩阵（真实产品里不存在这个控件） |
| Token 纪律 | 本区块新增样式**全部引 `--ws-*` 变量**，变量名/值对齐 `packages/ui/src/tokens.ts` 的 `roles.dark`；文件上半部分旧稿的裸 hex 属历史遗留，按需分批迁移（未在本次一次性重写） |

### 待用户拍板（原型未定死的交互）

1. **保存后是否给「重启入口」**：当前只在页面提示「需重启 upload-service 才生效」，**不提供**一键重启（避免普通设置页拿到重启服务的危险能力）。若希望提供，建议形态为「复制重启命令」或跳转到服务管理页定位到 web-upload。
2. **谁可以改目录**：原型沿用现有权限（`settings:edit` → admin 可改）。是否收紧为**仅 super_admin** 可改（与「浏览」同档）、admin 只读？——需要产品定；后端改权限码是一行（`@RequirePermission`）。
3. **是否暴露原始配置键**：页面只呈现语义化的「上传根目录」，不显示 `storage.upload_dir` 这个键名。若运维习惯直接找键名，可在卡头补一行 mono 提示。
4. **被拒绝的浏览是否留痕**：当前后端只有成功浏览才写审计（`view_storage_tree`）；「谁试图浏览被拒」不记录。属后端小改，不阻塞本页。

## 二、独立交互质检（逐条）

### A. 信息架构
- [x] 页面清单完整：本次只涉及 1 屏（系统设置），已在导航「基础设施 → 系统设置」可达
- [x] 入口可达、有返回路径：顶部一级「基础设施」→ 侧栏「系统设置」；其余菜单不回归（实跑验证 `dc('settings')` 后 active 屏为 `dc-settings`）
- [x] 导航层级 ≤ 3：一级（顶部）→ 二级（侧栏）→ 内容区，无跳转环

### B. 任务流闭环
- [x] 主路径闭环：树里逐级进入目录 → 「使用此目录」→ 切/manual 输入可见同值 → 「校验」通过 → 「保存」→ 徽标变「待重启生效」+ warn 条（**浏览器实跑通过**）
- [x] 异常分支：越界（`UPLOAD_DIR_OUT_OF_SCOPE` → 保存禁用）、目录不存在（允许保存 +「会自动创建」说明）、非超管/浏览关闭（树禁用 + 原因 + 落回输入）、空目录（说明 + 指引）、超量截断（提示行）、加载中（骨架）
- [x] 跨步衔接：选择 → 校验 → 保存 → 「待生效」状态与「当前生效」并列展示，状态可预期

### C. 状态矩阵
- [x] 加载中：读取/校验用骨架 + 文案（`setTimeout` 模拟 320ms / 500ms / 600ms 时序）
- [x] 空态 / 错误态 / 无权限：均有可见说明与唯一出路（「使用此目录」/ 切手动输入）
- [x] 完成态反馈：保存后 `toast('已保存，将在 upload-service 重启后生效')` + 徽标/双值变化

### D. 操作与确认
- [x] 主操作一屏 ≤ 1：存储卡 1 个 primary；通知卡保存为次要按钮
- [x] 破坏性操作二次确认：**本次无破坏性操作**（改目录可回退：再改回或删下发文件；页面已写「重启生效」语义）
- [x] 禁用/进行中：保存按钮在未校验/校验未通过时禁用；校验与保存期间按钮不可重复点（loading 态替换正文）
- [x] 表单校验有文案与字段级提示：校验结果块含 `解析结果 / 存在·可写 / 剩余空间 / 稳定错误码 / 文案`

### E. 视觉与 Token 一致性
- [x] 新增样式全部引 `--ws-*`（`:root` 内定义，值对齐 `packages/ui/src/tokens.ts` `roles.dark`）；无新裸 hex/rgba；语义色经 `--ws-danger/--ws-warn/--ws-ok`
- [x] 主色与目标端匹配：深色控制台壳，品牌橙 `--ws-brand-500`；深浅不混用
- [x] 无 emoji 图标；图标为内联 SVG；间距取 8 的倍数
- [x] 字号三级表达层级（19 / 13 / 12），不靠加粗堆叠

### F. 可访问性基线
- [x] 可读性：深底浅字，正文 13px、说明 12px，不低于基础值
- [x] 键盘/焦点：可点击元素用 `<button>`（状态条、使用此目录、校验、保存）；目录行用 `div`（原型阶段标注：落地应改 `button`/`role="button"` + `tabindex`）
- [x] 语义：主操作与次操作用按钮语义，非裸 div

### G. 内容与真实性
- [x] 文案为真实业务文案（路径样例用真实形态 `/Users/geekwen/web_system/uploads`、错误码用真实 `UPLOAD_DIR_OUT_OF_SCOPE`）
- [x] 信息完整：当前生效 / 待生效 / 来源 / 启动时间 / 校验四项 / 重启语义，均可见

### H. 目标端一致性
- [x] 目标端已确认并回显：桌面 Web（本记录第一节）
- [x] 壳与宿主元素匹配：桌面侧栏 + 顶栏 + 内容区，无移动端元素混入
- [x] 平台原生能力占位：无（纯 Web 表单，不涉及分享/授权/支付）
- [x] 未混入落地代码（本稿只改 `docs/ui/prototypes/`）

### I. 未定义项显式标注
- [x] 4 项待拍板已列在上方「待用户拍板」，未在稿里静默定死（尤其「是否提供重启入口」「谁可改」「是否收紧权限」）

### 遗留项（不阻塞确认，落地时处理）
1. 目录行 `div` → 落地改按钮语义 + 键盘可达（F 项标注）
2. 「保存后重启」的操作闭环（拍板项 1）——定了再补交互
3. `[原型] 状态演示` 控件落地时删除（不得进产品）

## 三、验证证据（独立于「看起来 OK」）

- 语法/结构：两个 `<script>` 块 `node --check` 通过；`#dc-settings` 存在；`stRender/stSetState/stValidate/stSave/stBrowse` 均已接线
- **浏览器实跑**（playwright-cli + 本地静态服务；`file:` 协议被禁，改用 `http://127.0.0.1:18100`）：
  | 断言 | 结果 |
  |---|---|
  | 用户路径（顶部「基础设施」→ 侧栏「系统设置」） | active 屏 = `dc-settings` ✅ |
  | 正常态渲染 | 徽标「已生效」+ 双值一致 + 权威来源（含 upload-service 启动时间）✅ |
  | 树导航（进入 `/web_system` → `/web_system/uploads`） | 面包屑与目录行正确，加载态先出现骨架 ✅ |
  | 「使用此目录」→ 切手动输入 | 输入框同步为所选路径 ✅ |
  | 校验失败（`/Volumes/...`） | 结果块显示 `UPLOAD_DIR_OUT_OF_SCOPE`，**保存禁用** ✅ |
  | 目录不存在（`.../uploads-2026`） | 提示「可直接保存，保存时会自动创建」，**保存可用** ✅ |
  | 保存（改到 `/Users/geekwen/data/uploads`） | 徽标 →「待重启生效」、待生效≠当前生效、warn 条出现、toast 提示 ✅ |
  | 非超管 | 树按钮禁用、模式自动落回手动输入、提示权限点 ✅ |
  | 空目录 / 截断 | 空态文案与「仅显示前 200 项」提示均出现 ✅ |
  | 状态演示条 | 9 态单行显示（修复了首版的换行挤压）✅ |
- 说明：截图仅用于本次自检（未入库）；评审请直接打开原型文件走查。
